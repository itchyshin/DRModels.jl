import{_ as a,o as t,c as i,ao as o}from"./chunks/framework.DB0mTVWt.js";const h=JSON.parse('{"title":"API stability","description":"","frontmatter":{},"headers":[],"relativePath":"api-stability.md","filePath":"api-stability.md","lastUpdated":null}'),s={name:"api-stability.md"};function n(r,e,l,d,c,p){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="API-stability" tabindex="-1">API stability <a class="header-anchor" href="#API-stability" aria-label="Permalink to &quot;API stability {#API-stability}&quot;">​</a></h1><p>This page explains which parts of DRModels.jl you can write into a long-lived analysis and which parts may still change as the package develops.</p><h2 id="What-stays-stable" tabindex="-1">What stays stable <a class="header-anchor" href="#What-stays-stable" aria-label="Permalink to &quot;What stays stable {#What-stays-stable}&quot;">​</a></h2><p>From <code>v0.7.0</code>, the <strong>Stable</strong> interface keeps its names, meanings, and conventions across the <code>0.7.x</code> line and beyond. This includes:</p><ul><li><p>the <code>bf()</code> formula grammar and the <code>drm()</code> fitting function;</p></li><li><p>the fourteen response-family constructors;</p></li><li><p>structured-effect markers such as <code>phylo()</code>, <code>spatial()</code>, <code>animal()</code>, and <code>relmat()</code>; and</p></li><li><p>the documented coefficient, inference, prediction, and plotting functions.</p></li></ul><p>The parameter conventions also stay the same: residual scale is <code>sigma</code> (never <code>tau</code>), bivariate residual correlation is <code>rho12</code>, and known sampling variance for meta-analysis is supplied with <code>meta_V()</code>.</p><p>A future breaking change to this interface would require a major, clearly announced version change. It will not arrive silently in a routine <code>0.7.x</code> update.</p><h2 id="What-is-still-experimental" tabindex="-1">What is still experimental <a class="header-anchor" href="#What-is-still-experimental" aria-label="Permalink to &quot;What is still experimental {#What-is-still-experimental}&quot;">​</a></h2><p>Experimental functions work for their documented uses, but their arguments, return values, or supported model classes may change between releases. They include:</p><ul><li><p><code>r2_constant_sigma()</code>, while broader definitions of marginal and conditional R² for random-effect models are still being designed;</p></li><li><p>the R bridge (<code>drm_bridge</code>, <code>drm_bridge_inference</code>, and <code>drm_listwise</code>);</p></li><li><p>cross-family and staged-pair tools (<code>mf_*</code>, <code>associate_pairs</code>, <code>latent_normal</code>, <code>association</code>, <code>PairAssociation</code>, and <code>integration_diagnostics</code>);</p></li><li><p>penalised phylogenetic fits, bivariate meta-analysis, and the variational approximation selected by <code>marginal = :VA</code>; and</p></li><li><p>joint models for missing predictors, including <code>mi()</code> and the prepared-model helpers.</p></li></ul><p>If you use one of these in an analysis that must be reproducible for several years, record the DRModels.jl version and read its release notes before updating.</p><h2 id="Engine-functions" tabindex="-1">Engine functions <a class="header-anchor" href="#Engine-functions" aria-label="Permalink to &quot;Engine functions {#Engine-functions}&quot;">​</a></h2><p>Some exported names are low-level computational building blocks for advanced scripts and benchmarks. Examples include <code>AugProblem</code>, <code>make_problem</code>, <code>fit_q4_sparse_tmb</code>, <code>estep_mode</code>, tree utilities, and parameter packers. They are not part of the stable analysis interface. Most readers should use <code>bf()</code>, <code>drm()</code>, and the documented post-fit functions instead.</p><h2 id="What-the-promise-covers" tabindex="-1">What the promise covers <a class="header-anchor" href="#What-the-promise-covers" aria-label="Permalink to &quot;What the promise covers {#What-the-promise-covers}&quot;">​</a></h2><p>The stability promise covers function names, argument meanings, and documented return conventions. It does not require every optimiser to follow the same numerical path: improvements to tolerances or algorithms may move estimates within the model&#39;s documented numerical accuracy.</p><p>It also does not turn the existence of a confidence-interval function into a general interval-coverage claim. Coverage depends on the model, sample size, parameter, and data-generating process. Read the limits on the relevant model page and assess the fitted model for your own study.</p><h2 id="Current-exclusions" tabindex="-1">Current exclusions <a class="header-anchor" href="#Current-exclusions" aria-label="Permalink to &quot;Current exclusions {#Current-exclusions}&quot;">​</a></h2><ul><li><p>Bivariate Student-t models do not accept <code>phylo</code>, <code>relmat</code>, <code>animal</code>, or <code>spatial</code> structured effects. Bivariate LogNormal models do support the structured effects listed on their model page because they are fit as Gaussian models on <code>log(y)</code>.</p></li><li><p>Joint missing-predictor functions remain Experimental even though they are available through the public module.</p></li></ul><p>For model-by-model boundaries, use <a href="./capabilities">Detailed capabilities and limits</a>. For a runnable first model, start with <a href="./getting-started">Getting started</a>.</p>',19)])])}const u=a(s,[["render",n]]);export{h as __pageData,u as default};
