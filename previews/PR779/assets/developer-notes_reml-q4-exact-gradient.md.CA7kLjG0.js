import{_ as n,C as c,o as l,c as r,ao as a,j as t,a as i,E as d,w as s}from"./chunks/framework.DqWL2xRk.js";const k=JSON.parse('{"title":"The exact gradient of the q=4 REML objective","description":"","frontmatter":{},"headers":[],"relativePath":"developer-notes/reml-q4-exact-gradient.md","filePath":"developer-notes/reml-q4-exact-gradient.md","lastUpdated":null}'),h={name:"developer-notes/reml-q4-exact-gradient.md"},g={class:"jldocstring custom-block",open:""},p={class:"jldocstring custom-block",open:""},_={class:"jldocstring custom-block",open:""};function m(u,e,b,y,f,v){const o=c("Badge");return l(),r("div",null,[e[12]||(e[12]=a(`<h1 id="The-exact-gradient-of-the-q4-REML-objective" tabindex="-1">The exact gradient of the q=4 REML objective <a class="header-anchor" href="#The-exact-gradient-of-the-q4-REML-objective" aria-label="Permalink to &quot;The exact gradient of the q=4 REML objective {#The-exact-gradient-of-the-q4-REML-objective}&quot;">​</a></h1><p>Developer note for <code>src/reml_q4.jl</code>. Written <strong>before</strong> the implementation (symbolic-alignment discipline): the maths is fixed here first, and every term is then mapped, term-by-term, onto a named quantity the code already computes or a named quantity to build.</p><p>Motivating defect: <code>fit_q4_reml</code> certified convergence on a <strong>central finite-difference</strong> gradient with step <code>h_inner = max(h_fd, 5e-4)</code> (<code>src/reml_q4.jl:420</code>, <code>fg!</code> at <code>:422–460</code>). On the <code>biv-q4-phylo-reml</code> fixture the run-to-run noise of the objective is ~1e-3, i.e. the same order as <code>g_tol = 1e-3</code>, so the certification was at or below the FD/noise floor. Every basin/polish strategy tried against that certification plateaued (<code>scratchpad/p12a-summary.md</code>, <code>p12a-basin-summary.md</code>).</p><hr><h2 id="1.-The-objective-as-implemented" tabindex="-1">1. The objective as implemented <a class="header-anchor" href="#1.-The-objective-as-implemented" aria-label="Permalink to &quot;1. The objective as implemented {#1.-The-objective-as-implemented}&quot;">​</a></h2><p><code>reml_ll_and_mode</code> (<code>src/reml_q4.jl:163–284</code>) returns, at outer parameters <code>φ = (β_ρ[kr], lc[10])</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">line</th><th style="text-align:right;">code</th><th style="text-align:right;">symbol</th></tr></thead><tbody><tr><td style="text-align:right;"><code>:167</code></td><td style="text-align:right;"><code>Lam = lc_to_Λ(lc)</code></td><td style="text-align:right;"><code>Λ(lc)</code> — 4×4 among-axis covariance, log-Cholesky parameterised</td></tr><tr><td style="text-align:right;"><code>:168</code></td><td style="text-align:right;"><code>P = prior_precision(Q_cond, inv(Lam))</code></td><td style="text-align:right;"><code>P(lc) = Q ⊗ Λ⁻¹</code>, node-major, sparse</td></tr><tr><td style="text-align:right;"><code>:195–218</code></td><td style="text-align:right;">alternation <code>estep_mode</code> / <code>cond_newton_beta</code></td><td style="text-align:right;">joint profiled mode <code>(û, β̂)</code></td></tr><tr><td style="text-align:right;"><code>:226</code></td><td style="text-align:right;"><code>ml_ll = laplace_ll(...)</code></td><td style="text-align:right;"><code>ℓ_ML = −J(û,β̂;φ) − ½ logdet H_uu + ½ logdet P</code></td></tr><tr><td style="text-align:right;"><code>:247–266</code></td><td style="text-align:right;"><code>H_u_beta</code>, <code>H_beta_beta</code></td><td style="text-align:right;"><code>B = H_uβ</code>, <code>D = H_ββ</code></td></tr><tr><td style="text-align:right;"><code>:269–273</code></td><td style="text-align:right;"><code>C = ch_H \\ H_u_beta</code>; <code>S = D − B&#39;C</code></td><td style="text-align:right;"><code>C = A⁻¹B</code>, <code>S = D − B&#39;A⁻¹B</code></td></tr><tr><td style="text-align:right;"><code>:281</code></td><td style="text-align:right;"><code>ld_S = logdet(ch_S)</code></td><td style="text-align:right;"><code>logdet S</code></td></tr><tr><td style="text-align:right;"><code>:283</code></td><td style="text-align:right;"><code>ml_ll - 0.5*ld_S</code></td><td style="text-align:right;"><code>L_REML(φ)</code></td></tr></tbody></table><p>with <code>A ≡ H_uu</code> (the CHOLMOD factor <code>ch_H</code> from <code>estep_mode</code>), and <code>_reml_normalise</code> (<code>:302</code>) adding the constant <code>(n_β/2)·log 2π</code>, which has zero gradient and is ignored below.</p><h3 id="1.1-The-one-identity-the-whole-derivation-rests-on" tabindex="-1">1.1 The one identity the whole derivation rests on <a class="header-anchor" href="#1.1-The-one-identity-the-whole-derivation-rests-on" aria-label="Permalink to &quot;1.1 The one identity the whole derivation rests on {#1.1-The-one-identity-the-whole-derivation-rests-on}&quot;">​</a></h3><p><code>H_uβ</code> and <code>H_ββ</code> are built at <code>:247–266</code> from the <strong>same</strong> per-leaf 4×4 Hessian <code>Hb = leaf_hess(...)</code> as <code>H_uu</code>&#39;s leaf blocks, because every axis&#39;s fixed effect enters the leaf through <code>η_d + u_d</code> (the header comment at <code>:229–235</code>). Writing, for data row <code>i</code> at leaf node <code>t</code> (<code>base = 4(t−1)</code>), the <strong>lift</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>R_i : z = (u, β) ↦ 4-vector of axis totals,   R_i = [E_i | F_i]</span></span>
<span class="line"><span>E_i[d, base+d] = 1                (u part, 4 × n_u)</span></span>
<span class="line"><span>F_i[d, off[d]+k] = X_d[i,k]       (β part, 4 × n_β), X_1..X_4 = X1, X2, Xs1, Xs2</span></span></code></pre></div><p>the leaf&#39;s entire contribution to the joint Hessian over <code>z</code> is <code>R_iᵀ Hb R_i</code>. <code>off</code> and the per-axis designs are literally <code>off</code>/<code>Xax</code> at <code>:239–242</code>.</p><p>Therefore the <strong>joint Hessian over the augmented state</strong> <code>z = (u, β)</code> is</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>𝓗 = [ A   B ]      A = H_uu,  B = H_uβ,  D = H_ββ</span></span>
<span class="line"><span>    [ B&#39;  D ]      logdet 𝓗 = logdet A + logdet S</span></span></code></pre></div><p>and the implemented objective collapses to a single Laplace form:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>L_REML(φ) = −J(ẑ; φ) − ½ logdet 𝓗(ẑ; φ) + ½ logdet P(φ)          (★)</span></span></code></pre></div><p><code>J(z;φ)</code> is <code>joint_nll</code> with <code>β</code> promoted from a fixed argument to part of the state, and <code>½ logdet P</code> is unchanged (the flat prior on <code>β</code> contributes no <code>logdet</code>). <strong>(★) is structurally identical to the ML objective in <code>fit_q4_sparse_tmb.jl:10–15</code>, with <code>u → z</code> and <code>H → 𝓗</code>.</strong> That is what makes the existing exact-gradient machinery reusable term-for-term.</p><hr><h2 id="2.-The-gradient" tabindex="-1">2. The gradient <a class="header-anchor" href="#2.-The-gradient" aria-label="Permalink to &quot;2. The gradient {#2.-The-gradient}&quot;">​</a></h2><p>At the joint stationary point <code>∇_z J(ẑ;φ) = 0</code>, the implicit-function derivative of (★) is the exact analogue of <code>marginal_and_exact_grad</code> (<code>src/fit_q4_sparse_tmb.jl:288–434</code>). For the minimised objective <code>L⁻ = −L_REML</code> (what LBFGS sees, divided by <code>nobs</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>dL⁻/dφ_k = ∂/∂φ_k [ J + ½ logdet 𝓗 − ½ logdet P ]|_{ẑ frozen}      (CHEAP)</span></span>
<span class="line"><span>           − ∂/∂φ_k [ (∇_z J)ᵀ w ]|_{ẑ, w frozen}                   (IMPLICIT)</span></span>
<span class="line"><span>w = 𝓗⁻¹ v,   v = ½ ∇_z logdet 𝓗</span></span></code></pre></div><h3 id="2.1-Selected-inverse-of-𝓗-without-factorising-𝓗" tabindex="-1">2.1 Selected inverse of 𝓗 without factorising 𝓗 <a class="header-anchor" href="#2.1-Selected-inverse-of-𝓗-without-factorising-𝓗" aria-label="Permalink to &quot;2.1 Selected inverse of 𝓗 without factorising 𝓗 {#2.1-Selected-inverse-of-𝓗-without-factorising-𝓗}&quot;">​</a></h3><p>Never form or factor <code>𝓗</code>. With <code>C = A⁻¹B</code> (already computed, <code>:269–272</code>) and <code>S⁻¹</code> (already factored, <code>ch_S</code> at <code>:275</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>𝓗⁻¹ = [ A⁻¹ + C S⁻¹ Cᵀ   −C S⁻¹ ]</span></span>
<span class="line"><span>      [ −S⁻¹ Cᵀ            S⁻¹   ]</span></span></code></pre></div><p>Every entry needed is <code>A⁻¹</code> at the Takahashi pattern (<code>takahashi_selinv(ch_H)</code>, <code>src/takahashi_selinv.jl</code>, O(p)) plus a rank-<code>n_β</code> correction with <code>n_β = 6</code> on this fixture. Cost stays O(p·n_β²).</p><p>The single object each leaf needs is the 4×4</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Ω_i = R_i 𝓗⁻¹ R_iᵀ = Vsel[base+1:base+4, base+1:base+4] + G_i S⁻¹ G_iᵀ</span></span>
<span class="line"><span>G_i = C[base+1:base+4, :] − F_i          (4 × n_β)</span></span></code></pre></div><p>which is the REML replacement for the ML code&#39;s <code>Vblk</code> (<code>fit_q4_sparse_tmb.jl:343</code>). <strong><code>Ω_i = Vblk</code> exactly when <code>F_i = 0</code> and <code>S⁻¹ = 0</code>, i.e. when nothing is marginalised — the ML reduction.</strong></p><h3 id="2.2-Alignment-table-—-every-term,-its-symbol,-its-code-home,-its-scale" tabindex="-1">2.2 Alignment table — every term, its symbol, its code home, its scale <a class="header-anchor" href="#2.2-Alignment-table-—-every-term,-its-symbol,-its-code-home,-its-scale" aria-label="Permalink to &quot;2.2 Alignment table — every term, its symbol, its code home, its scale {#2.2-Alignment-table-—-every-term,-its-symbol,-its-code-home,-its-scale}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">#</th><th style="text-align:right;">Term</th><th style="text-align:right;">Symbol</th><th style="text-align:right;">Scale / parameterisation</th><th style="text-align:right;">Code home</th></tr></thead><tbody><tr><td style="text-align:right;">C1</td><td style="text-align:right;"><code>∇_φ J(ẑ;φ)</code></td><td style="text-align:right;"><code>∂J/∂β_ρ</code>, <code>∂J/∂lc</code></td><td style="text-align:right;"><code>β_ρ</code> identity link (<code>ηr = Xr·β_ρ</code>, <code>ρ = RHO_GUARD·tanh ηr</code>); <code>lc</code> log-Cholesky</td><td style="text-align:right;"><strong>to build</strong>: <code>reml_joint_nll_T(prob, P_t, û, β̂_t)</code> — copy of <code>joint_nll_T</code> (<code>fit_q4_sparse_tmb.jl:442</code>) with <code>β.rho</code> type-generic; single-level ForwardDiff over <code>φ</code>, mirrors <code>jn_of_θ</code> (<code>:319–325</code>)</td></tr><tr><td style="text-align:right;">C2</td><td style="text-align:right;"><code>−½ ∇_lc logdet P = +½·N·∇_lc logdet Λ</code></td><td style="text-align:right;"><code>glogdetΛ</code></td><td style="text-align:right;">log-Cholesky; <code>N = prob.n_total</code></td><td style="text-align:right;"><strong>exists verbatim</strong>: <code>fit_q4_sparse_tmb.jl:330–332</code></td></tr><tr><td style="text-align:right;">C3a</td><td style="text-align:right;"><code>½ tr(𝓗⁻¹ ∂𝓗/∂β_ρ)</code></td><td style="text-align:right;"><code>sη[5]</code> via <code>Ω_i</code></td><td style="text-align:right;">identity link on <code>β_ρ</code>, chained through <code>Xr[i,:]</code></td><td style="text-align:right;"><strong>adapt</strong>: <code>fit_q4_sparse_tmb.jl:340–367</code>, replacing <code>Vblk → Ω_i</code>; only the <code>m = 5</code> (<code>ηr</code>) column is needed, since <code>β_μ</code>/<code>β_σ</code> are no longer outer parameters</td></tr><tr><td style="text-align:right;">C3b</td><td style="text-align:right;"><code>½ tr(𝓗⁻¹ ∂𝓗/∂lc_k)</code></td><td style="text-align:right;"><code>Gst</code> ⊗ <code>Mk = −Λ⁻¹(∂Λ/∂lc_k)Λ⁻¹</code></td><td style="text-align:right;">log-Cholesky</td><td style="text-align:right;"><strong>adapt</strong>: <code>fit_q4_sparse_tmb.jl:374–396</code>; <code>Gst</code> accumulates <code>W_uu = A⁻¹ + C S⁻¹ Cᵀ</code> at the <code>Q_cond</code> pattern instead of bare <code>Vsel</code> — one extra <code>q·(C[bt+a,:]ᵀ S⁻¹ C[bs+b,:])</code> per nonzero</td></tr><tr><td style="text-align:right;">I1</td><td style="text-align:right;"><code>v = ½ ∇_z logdet 𝓗</code></td><td style="text-align:right;"><code>v = (v_u, v_β)</code></td><td style="text-align:right;">natural <code>z</code> scale</td><td style="text-align:right;"><strong>adapt</strong>: <code>fit_q4_sparse_tmb.jl:402–418</code>. Per leaf, <code>ṽ_c = ½ Σ_{a,b} Ω_i[a,b]·T[a,b,c]</code> with <code>T = leaf_hess_du</code> (<code>:264</code>); then <code>v[base+c] += ṽ_c</code> <strong>and</strong> <code>v_β[off[c]+k] += ṽ_c·X_c[i,k]</code> (the new β rows, from <code>R_iᵀ</code>)</td></tr><tr><td style="text-align:right;">I2</td><td style="text-align:right;"><code>w = 𝓗⁻¹ v</code></td><td style="text-align:right;"><code>w = (w_u, w_β)</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><strong>to build</strong>: block solve. <code>q = S⁻¹(Cᵀv_u − v_β)</code>; <code>w_u = A⁻¹v_u + C q</code> (one <code>ch_H \\</code>), <code>w_β = −q</code>. No 𝓗 factorisation</td></tr><tr><td style="text-align:right;">I3</td><td style="text-align:right;"><code>−∇_φ[(∇_z J)ᵀ w]</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>φ</code> scale</td><td style="text-align:right;"><strong>to build</strong>: <code>scalar_of_φ</code> mirroring <code>fit_q4_sparse_tmb.jl:424–430</code>; <code>∇_z J</code> = <code>joint_grad_T</code> for the <code>u</code> block <strong>plus</strong> the β block <code>∂J/∂β_d[k] = Σ_i leaf_grad(...)[d]·X_d[i,k]</code></td></tr><tr><td style="text-align:right;">N</td><td style="text-align:right;"><code>(n_β/2)·log 2π</code></td><td style="text-align:right;">—</td><td style="text-align:right;">constant</td><td style="text-align:right;"><code>_reml_normalise</code> (<code>:302</code>) — <strong>zero gradient</strong></td></tr><tr><td style="text-align:right;">Sc</td><td style="text-align:right;">objective scaling</td><td style="text-align:right;"><code>/nobs</code></td><td style="text-align:right;"><code>nobs = length(prob.leaf_node)</code></td><td style="text-align:right;">matches <code>neg_reml</code> (<code>:414</code>), so the returned gradient must be divided by <code>nobs</code> too</td></tr></tbody></table><h3 id="2.3-Where-the-FD-gradient-was-wrong,-precisely" tabindex="-1">2.3 Where the FD gradient was wrong, precisely <a class="header-anchor" href="#2.3-Where-the-FD-gradient-was-wrong,-precisely" aria-label="Permalink to &quot;2.3 Where the FD gradient was wrong, precisely {#2.3-Where-the-FD-gradient-was-wrong,-precisely}&quot;">​</a></h3><p>The FD gradient perturbs <code>φ_k</code> by <code>5e-4</code> and <strong>re-runs the whole alternation</strong> (<code>fg!</code>, <code>:435–443</code>), so each one-sided evaluation carries the alternation&#39;s own <code>1e-4·(1+‖β‖)</code> exit slack (<code>:216</code>) plus the mode-finder&#39;s residual. The exact gradient (★) never re-solves: it evaluates one stationary point and applies the implicit-function theorem, so its accuracy is set by <code>‖∇_z J(ẑ)‖</code>, not by a difference of two noisy solves.</p><h3 id="2.4-Consequence-for-certification" tabindex="-1">2.4 Consequence for certification <a class="header-anchor" href="#2.4-Consequence-for-certification" aria-label="Permalink to &quot;2.4 Consequence for certification {#2.4-Consequence-for-certification}&quot;">​</a></h3><p>(★) is exact <strong>only at a joint stationary point of <code>J</code> over <code>z = (u, β)</code></strong>. The existing alternation exits on a relative β criterion (<code>:216</code>) that leaves <code>‖∇_β J‖</code> at ~1e-4·‖β‖ — enough to poison an exact gradient certified at 1e-6. The fix is already paid for: <code>𝓗</code> is exactly the joint Newton matrix, and §2.1 gives a block solve against it for free. A handful of joint Newton steps</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>z ← z − 𝓗⁻¹ ∇_z J(z)        (same block solve as I2)</span></span></code></pre></div><p>drives <code>‖∇_z J‖</code> to ~1e-10, after which the exact gradient is trustworthy and convergence can be certified on it. This is the &quot;Newton-grade certification&quot; the FD route could not provide.</p><hr><h2 id="3.-Found-while-implementing:-the-diagonal-Λ-pattern-degeneracy" tabindex="-1">3. Found while implementing: the diagonal-Λ pattern degeneracy <a class="header-anchor" href="#3.-Found-while-implementing:-the-diagonal-Λ-pattern-degeneracy" aria-label="Permalink to &quot;3. Found while implementing: the diagonal-Λ pattern degeneracy {#3.-Found-while-implementing:-the-diagonal-Λ-pattern-degeneracy}&quot;">​</a></h2><p><code>prior_precision(Q, Λ⁻¹)</code> (<code>src/sparse_aug_plsm.jl:79</code>) calls <code>sparse(Λinv)</code>, which <strong>drops zeros</strong>. At an exactly diagonal <code>Λ</code> — including <code>fit_q4_reml</code>&#39;s own default warm start <code>Λ0 = 0.3I(4)</code> (<code>src/reml_q4.jl:374</code>) — the cross-axis entries of <code>P</code>, and hence of <code>H_uu</code> at every non-leaf node, are <em>structurally</em> absent. The Cholesky pattern shrinks with them, so the Takahashi selected inverse cannot return <code>A⁻¹</code> at entries the <code>logdet H</code> trace genuinely needs. They are missing, not zero.</p><p>Measured on <code>biv-q4-phylo-reml</code> at <code>Λ = 0.3I</code>, exact vs a step-scanned central difference: two <code>lc</code> components were wrong by <strong>0.037</strong> and <strong>13.70</strong>; with a <code>1e-6</code> off-diagonal added to <code>Λ</code>, <strong>every</strong> component agreed to <strong>6.7e-8</strong>.</p><p>Fix in the REML path (at the time): a local helper, <code>_reml_prior_precision</code>, stored all 16 entries of the 4×4 axis block explicitly. The matrix was numerically identical; only the pattern changed.</p><p><strong><code>marginal_and_exact_grad</code> (<code>fit_q4_sparse_tmb.jl:374–384</code>) builds its <code>Gst</code> from the same construction and therefore shares this degeneracy on the ML path.</strong> This was outside the scope of the original REML repair, but it should be checked: the ML fit also starts at <code>Λ0 = 0.3I</code>, so its very first exact gradient is taken at the degenerate point.</p><p><strong>Update:</strong> <code>prior_precision</code> itself (<code>src/sparse_aug_plsm.jl</code>) now stores the full q×q axis block unconditionally, so it no longer drops the cross-axis entries at a diagonal <code>Λ</code>. That made the local <code>_reml_prior_precision</code> guard redundant: the two builders produced bit-identical sparse output on this fixture (same pattern, same <code>nzval</code>, <code>nnz</code> = 1376 at both a diagonal and a non-diagonal <code>Λ</code>), and the redundant helper was removed while its call sites — including <code>reml_ll_and_mode</code> (<code>src/reml_q4.jl:309</code>), whose call was previously the only one left on the unguarded <code>prior_precision</code> — were repointed to <code>prior_precision</code> directly. Every call site in <code>src/reml_q4.jl</code> now uses the same, now-correct, <code>prior_precision</code>.</p><hr><h2 id="4.-API" tabindex="-1">4. API <a class="header-anchor" href="#4.-API" aria-label="Permalink to &quot;4. API {#4.-API}&quot;">​</a></h2><p>The two module-level entry points the exact path exposes (unexported; called by <code>fit_q4_reml</code>&#39;s <code>fg!</code>). Both take the augmented problem, the root-conditioned tree precision <code>Q_cond</code>, and the outer parameter vector <code>φ = (β_ρ, lc)</code>.</p>`,46)),t("details",g,[t("summary",null,[e[0]||(e[0]=t("a",{id:"DRModels.reml_nll_exact",href:"#DRModels.reml_nll_exact"},[t("span",{class:"jlbinding"},"DRModels.reml_nll_exact")],-1)),e[1]||(e[1]=i()),d(o,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[3]||(e[3]=a('<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">reml_nll_exact</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prob, Q_cond, phi; u0, beta0, n_newton) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Float64</span></span></code></pre></div><p>The q=4 REML objective as a NEGATIVE, UNNORMALISED restricted log-likelihood at <code>phi = (beta_rho, lc)</code>, evaluated at a joint-Newton-certified <code>(û, β̂)</code>. Same objective as <code>reml_ll_and_mode</code> up to sign; the difference is that the joint mode is driven to <code>‖∇_z J‖ ≈ 1e-10</code> instead of the alternation&#39;s relative beta criterion, which makes the value a smooth, deterministic function of <code>phi</code> — what a finite-difference reference (and a Newton-grade certification) needs. Returns <code>Inf</code> on the non-PD Schur barrier.</p>',2)),d(o,{type:"info",class:"source-link",text:"source"},{default:s(()=>[...e[2]||(e[2]=[t("a",{href:"https://github.com/itchyshin/DRModels.jl/blob/c648a8ba45787115da932c52df57b36ced7f33ab/src/reml_q4.jl#L449-L459",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),t("details",p,[t("summary",null,[e[4]||(e[4]=t("a",{id:"DRModels.reml_nll_and_exact_grad",href:"#DRModels.reml_nll_and_exact_grad"},[t("span",{class:"jlbinding"},"DRModels.reml_nll_and_exact_grad")],-1)),e[5]||(e[5]=i()),d(o,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[7]||(e[7]=a(`<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">reml_nll_and_exact_grad</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prob, Q_cond, phi; u0, beta0, n_newton)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    -&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (nll, grad, û, β̂, ch_H, z_residual)</span></span></code></pre></div><p>Negative unnormalised REML log-likelihood and its EXACT gradient over <code>phi = (beta_rho, lc)</code>. <code>z_residual</code> is <code>‖∇_z J(ẑ)‖</code> — the exact gradient is valid only where this is small, so callers should check it before certifying convergence on <code>grad</code>.</p>`,2)),d(o,{type:"info",class:"source-link",text:"source"},{default:s(()=>[...e[6]||(e[6]=[t("a",{href:"https://github.com/itchyshin/DRModels.jl/blob/c648a8ba45787115da932c52df57b36ced7f33ab/src/reml_q4.jl#L472-L480",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),t("details",_,[t("summary",null,[e[8]||(e[8]=t("a",{id:"DRModels.reml_objective_at",href:"#DRModels.reml_objective_at"},[t("span",{class:"jlbinding"},"DRModels.reml_objective_at")],-1)),e[9]||(e[9]=i()),d(o,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[11]||(e[11]=a(`<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">reml_objective_at</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prob, Q_cond, phi; beta0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, u0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n_newton</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    -&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NamedTuple</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(reml_loglik, raw_reml_ll, beta, u, converged)</span></span></code></pre></div><p>Evaluate the q=4 REML objective at a SUPPLIED <code>phi = (beta_rho, lc)</code> — the variance-component / correlation half of <code>fit_q4_reml</code>&#39;s parameter space — reprofiling <code>beta_mu1/mu2/s1/s2</code> (the marginalised mean and scale fixed effects) by the SAME conditional-Newton alternation <code>fit_q4_reml</code> runs internally (<code>reml_ll_and_mode</code>), rather than re-running the outer LBFGS.</p><p>This is the diagnostic primitive behind cross-engine mode-finder-vs- objective-translation checks (#575): given another engine&#39;s fitted point (mapped into DRModels.jl&#39;s <code>phi</code>/<code>beta</code> scale — see <code>pack_phi</code>, <code>Λ_to_lc</code>), call this to ask &quot;is DRModels.jl&#39;s OWN objective, evaluated AT that point, better or worse than what DRModels.jl&#39;s own solver returned?&quot; without hand-writing the inner E-step/Newton alternation each time.</p><p><code>beta0</code>/<code>u0</code> seed the conditional-Newton warm start (pass the other engine&#39;s fixed effects, or <code>fit_q4_reml</code>&#39;s returned <code>.beta</code>/<code>.u_hat</code>, as <code>beta0</code>/<code>u0</code> respectively) so the inner profile has the best chance of reaching its true conditional optimum at <code>phi</code> rather than stalling from a cold start.</p><p><code>reml_loglik</code> is on the SAME normalised (Patterson–Thompson, <code>+ (n_β/2)·log(2π)</code>) scale as <code>fit_q4_reml</code>&#39;s <code>.reml_loglik</code> and as drmTMB/TMB/lme4/glmmTMB (#477); <code>raw_reml_ll</code> is the pre-normalisation value, exposed for constant-offset sanity checks. <code>n_β</code> is <code>prob</code>&#39;s combined <code>X1+X2+Xs1+Xs2</code> design width — the same marginalised-fixed-effect count <code>fit_q4_reml</code> uses.</p><p>Returns <code>(reml_loglik, raw_reml_ll, beta, u, converged)</code>. <code>converged</code> is the inner alternation&#39;s own convergence flag (<code>inner_converged</code> from <code>reml_ll_and_mode</code>) — a barrier hit (non-PD Schur complement) surfaces as <code>raw_reml_ll = reml_loglik = -Inf</code>, <code>converged = false</code>, rather than an error.</p>`,6)),d(o,{type:"info",class:"source-link",text:"source"},{default:s(()=>[...e[10]||(e[10]=[t("a",{href:"https://github.com/itchyshin/DRModels.jl/blob/c648a8ba45787115da932c52df57b36ced7f33ab/src/reml_q4.jl#L639-L671",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})])])}const w=n(h,[["render",m]]);export{k as __pageData,w as default};
