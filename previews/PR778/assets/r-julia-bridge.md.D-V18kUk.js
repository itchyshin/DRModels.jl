import{_ as s,o as a,c as i,ao as t}from"./chunks/framework.DqN8xua4.js";const u=JSON.parse('{"title":"R ↔ Julia bridge","description":"","frontmatter":{},"headers":[],"relativePath":"r-julia-bridge.md","filePath":"r-julia-bridge.md","lastUpdated":null}'),n={name:"r-julia-bridge.md"};function o(l,e,r,d,p,h){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="R-Julia-bridge" tabindex="-1">R ↔ Julia bridge <a class="header-anchor" href="#R-Julia-bridge" aria-label="Permalink to &quot;R ↔ Julia bridge {#R-Julia-bridge}&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">Status — experimental optional bridge</p><p>DRModels.jl exposes <code>drm_bridge()</code>, a marshalling-friendly entry point used by the optional <code>drmTMB(formula, ..., engine = &quot;julia&quot;)</code> backend for supported models. The companion R glue lives in the <strong>drmTMB R repository</strong> via <a href="https://github.com/JuliaInterop/JuliaCall" target="_blank" rel="noreferrer">JuliaCall</a>; the default <code>engine = &quot;tmb&quot;</code> does not require Julia.</p></div><h2 id="Start-with-your-current-workflow" tabindex="-1">Start with your current workflow <a class="header-anchor" href="#Start-with-your-current-workflow" aria-label="Permalink to &quot;Start with your current workflow {#Start-with-your-current-workflow}&quot;">​</a></h2><p>Stay with native <code>drmTMB</code> when you want its default, established R/TMB route. Choose the bridge only when your model is within its admitted scope and you want to try the Julia backend without rewriting your formula or data workflow.</p><p>If you want to keep writing R, follow drmTMB&#39;s <a href="https://itchyshin.github.io/drmTMB/articles/julia-engine.html" target="_blank" rel="noreferrer">Julia-engine setup and ordinary-regression example</a>. It installs Julia and JuliaCall, points R to a local DRModels.jl checkout, and then fits the same R formula with <code>engine = &quot;julia&quot;</code>. Keep <code>engine = &quot;tmb&quot;</code> until that optional setup is complete.</p><p>If you want to write Julia directly, begin with <a href="./getting-started">Getting started</a>, then use the <a href="./rosetta">Rosetta</a> to translate a model you already understand in R.</p><h2 id="When-native-R-is-the-better-route" tabindex="-1">When native R is the better route <a class="header-anchor" href="#When-native-R-is-the-better-route" aria-label="Permalink to &quot;When native R is the better route {#When-native-R-is-the-better-route}&quot;">​</a></h2><p>Keep <code>engine = &quot;tmb&quot;</code> when your study needs an established R workflow that is outside this bridge. In particular, use drmTMB&#39;s <a href="https://itchyshin.github.io/drmTMB/articles/spatial-models.html" target="_blank" rel="noreferrer">Coordinate-spatial structured effects</a> for its distributional-regression spatial route; use gllvmTMB&#39;s <a href="https://itchyshin.github.io/gllvmTMB/articles/spatial-models.html" target="_blank" rel="noreferrer">Multivariate spatial models with an SPDE mesh</a>, <a href="https://itchyshin.github.io/gllvmTMB/articles/temporal-ar1.html" target="_blank" rel="noreferrer">Temporal covariance for repeated multivariate measurements</a>, or <a href="https://itchyshin.github.io/gllvmTMB/articles/integrated-repeated-visits.html" target="_blank" rel="noreferrer">What do repeated survey visits add to an integrated model?</a> when those match your question. These native R spatial, temporal, and repeated-visit workflows are not admitted to <code>engine = &quot;julia&quot;</code>; a successful Julia setup does not extend the bridge to them.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>**Admitted fixture-backed coefficient-scale parity** (opt-in</span></span>
<span class="line"><span>\`DRM_PARITY_TESTS=1\`, via \`drm_bridge\` + committed drmTMB generated</span></span>
<span class="line"><span>numbers only). The eleven cells below all record **drmTMB 0.7.0** in their</span></span>
<span class="line"><span>\`expected.meta.toml\` files:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Six baseline cells:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- \`gaussian-locscale\`</span></span>
<span class="line"><span>- \`gaussian-bivariate-rho12\`</span></span>
<span class="line"><span>- \`robust-student\`</span></span>
<span class="line"><span>- \`count-nbinom2\`</span></span>
<span class="line"><span>- \`proportion-beta\`</span></span>
<span class="line"><span>- \`meta-analysis-V\`</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Four additional fixed-effect cells:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- \`count-poisson\`</span></span>
<span class="line"><span>- \`positive-gamma\`</span></span>
<span class="line"><span>- \`binomial-trials\`</span></span>
<span class="line"><span>- \`positive-lognormal\`</span></span>
<span class="line"><span></span></span>
<span class="line"><span>One NB2 location–scale fixed-effect cell:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- \`nbinom2-dispersion\` (\`y ~ x; sigma ~ x\`)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>A separate seven-cell formula-construct cohort also uses drmTMB 0.7.0, but</span></span>
<span class="line"><span>it tests formula translation rather than expanding this coefficient-scale</span></span>
<span class="line"><span>cohort. The reference records a package-version anchor, not a unique</span></span>
<span class="line"><span>comparator source-build pin.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Historical measured warm wall-clock** (local machine; Julia \`drm_bridge\`</span></span>
<span class="line"><span>vs installed drmTMB **0.6.0** at the time; BLAS/OMP threads = 1; 1 warmup +</span></span>
<span class="line"><span>5 timed reps):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- The six baseline cells — the warm median favoured Julia on every cell measured.</span></span>
<span class="line"><span>- The four additional cells plus \`nbinom2-dispersion\` — likewise on every cell.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The timing artifacts were not re-measured on the 0.7.0 fixture anchor. They</span></span>
<span class="line"><span>are neither a general “Nx faster for all drmTMB models” claim nor the</span></span>
<span class="line"><span>verified q=4 PLSM single-fit cell at p=100, which</span></span>
<span class="line"><span>is a different measurement from the separate q=4 scaling comparison.</span></span>
<span class="line"><span>For translating R syntax to Julia by hand, see the [Rosetta page](rosetta.md).</span></span></code></pre></div><h2 id="The-idea" tabindex="-1">The idea <a class="header-anchor" href="#The-idea" aria-label="Permalink to &quot;The idea {#The-idea}&quot;">​</a></h2><p>Two ways to use DRModels.jl from R, in increasing integration:</p><ol><li><p><strong>Translate by hand</strong> — rewrite the model in Julia using <code>drm</code> / <code>bf</code>. The <a href="./rosetta">Rosetta</a> phrasebook is the lookup table. Available today.</p></li><li><p><strong><code>engine = &quot;julia&quot;</code></strong> — keep writing ordinary <code>drmTMB(...)</code> R code; drmTMB marshals the formula and data across JuliaCall, calls DRModels.jl to fit, and returns a result object shaped like a native drmTMB fit. Supported for Gaussian one-response and two-response models, the first Gaussian <code>phylo(1 | species)</code> mean bridge with constant <code>sigma</code>, location–scale–scale <code>sd(group)</code> / <code>sd(species, level = &quot;phylogenetic&quot;)</code> models (ML, REML, sparse scaling, and missing response inclusion), narrow complete-response q=2 structured Gaussian fixtures, and the eleven fixture-backed coefficient-scale cells listed above (Julia-side <code>drm_bridge</code> gate; R-side Lovelace glue remains in the drmTMB repo).</p></li></ol><h3 id="Trees-with-polytomies" tabindex="-1">Trees with polytomies <a class="header-anchor" href="#Trees-with-polytomies" aria-label="Permalink to &quot;Trees with polytomies {#Trees-with-polytomies}&quot;">​</a></h3><p>A polytomy is an internal node with more than two immediate children. The development bridge accepts these nodes without resolving them into invented binary branches. The tree must still have positive branch lengths, nonempty unique tip labels, and at least two children at every internal node. The R bridge currently requires an ultrametric tree for its correlation-scale convention. Zero-length branches and unary nodes remain separate parity work.</p><p>The development bridge preserves tip labels containing spaces, punctuation, Unicode and apostrophes. Keep the same labels in your data; do not replace spaces with underscores. The serializer quotes labels where needed, and Julia decodes them without changing their spelling. In direct Newick input, use single quotes around such labels and double an apostrophe inside a quoted label:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DRModels</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">named_tree </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> augmented_phy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;(&#39;Mola mola&#39;:1,&#39;O&#39;&#39;Brien&#39;:1,A_B:1);&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> named_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">leaf_names </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Mola mola&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;O&#39;Brien&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;A_B&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">named_tree</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">leaf_names</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">3-element Vector{String}:</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;Mola mola&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;O&#39;Brien&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;A_B&quot;</span></span></code></pre></div><p>Quoted labels preserve whitespace literally, including leading/trailing spaces. This concerns tip identity; internal-node labels are parsed but not retained.</p><p>Direct Julia keeps the supplied Brownian branch-length scale and can represent unequal tip depths:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DRModels</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">phy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> augmented_phy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;((A:1,B:2,C:3):4,D:5,E:6);&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> phy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n_leaves </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> phy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n_total </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 7</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> phylo_tree_height</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(phy) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 7</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Small diagnostic only: a dense tip covariance is unsuitable for large trees.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sigma_phy_dense</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(phy)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">5×5 Matrix{Float64}:</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> 5.0  4.0  4.0  0.0  0.0</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> 4.0  6.0  4.0  0.0  0.0</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> 4.0  4.0  7.0  0.0  0.0</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> 0.0  0.0  0.0  5.0  0.0</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> 0.0  0.0  0.0  0.0  6.0</span></span></code></pre></div><p>For an ultrametric tree of height <code>h</code>, Julia&#39;s raw phylogenetic SD multiplied by <code>sqrt(h)</code> is on the correlation scale used by the R bridge. One height cannot standardize a tree whose tip depths differ. Accepting a topology does not itself verify every response family, profile interval or bootstrap workflow.</p><h3 id="One-modelled-missing-predictor-—-development-admission" tabindex="-1">One modelled missing predictor — development admission <a class="header-anchor" href="#One-modelled-missing-predictor-—-development-admission" aria-label="Permalink to &quot;One modelled missing predictor — development admission {#One-modelled-missing-predictor-—-development-admission}&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">Experimental</p><p>Exported for evaluation. API and numerics may change; not covered by the R-parity scoreboard.</p></div><p>The R bridge also has a deliberately narrow development route for one modelled missing predictor. It accepts a Gaussian identity-link response, exactly one bare additive <code>mi(x)</code> term in <code>mu</code>, complete fixed-effect exogenous designs, and a Gaussian, Bernoulli, ordinal or categorical fixed-effect predictor model. The direct Julia frontend and <code>drmTMB(..., engine = &quot;julia&quot;)</code> use the same prepared joint likelihood; observed <code>x</code> values remain observed and missing <code>x</code> values are integrated rather than filled before fitting.</p><p>Use <code>impute = list(x = x ~ z)</code> for a Gaussian predictor, or <code>impute = list(x = impute_model(x ~ z, family = binomial()))</code> for a binary predictor, with either <code>missing = miss_control(response = &quot;drop&quot;, predictor = &quot;model&quot;)</code> or <code>miss_control(response = &quot;include&quot;, predictor = &quot;model&quot;)</code>. The bridge response-drop path removes missing-response rows before preparation. That is a documented preprocessing choice, not native response-policy parity.</p><p>This admission is not a general missing-data bridge. It rejects other response families, interactions or nesting involving <code>mi()</code>, random or structured effects, offsets, non-default controls, likelihood weights, and REML. <code>summary()</code> and Wald <code>confint()</code> are available only when the returned covariance is usable; profile and bootstrap intervals explicitly error. The Gaussian predictor-SD interval is a natural-scale delta-Wald interval, may cross zero, and is not claimed to match native intervals or to have established coverage.</p><p>Two public bridge-adapter cases pass. Training prediction and binary <code>newdata</code> handling have been repaired and checked independently. Full numerical parity remains open: small differences in native optimizer stopping affect coefficients and predictions beyond the declared tolerance. This route makes no full native-parity, speed, or interval-coverage claim. A separate development route also admits two independent Gaussian predictors; this does not admit arbitrary combinations of missing-predictor families.</p><p>For an ordered predictor, use an ordered R factor and <code>impute_model(x ~ z, family = cumulative_logit())</code>; for a nominal predictor, use a factor and <code>impute_model(x ~ z, family = categorical())</code>. Both finite-state routes require at least three observed levels. The ordered predictor model removes its intercept because its cutpoints already supply location parameters. The response mean still follows its own formula&#39;s intercept convention.</p><p>Finite-state predictions average the fitted mean over posterior states. <code>imputed()</code> returns expected category scores and conditional score SDs for an ordered predictor, or the first modal category code for a nominal predictor. Nominal metric standard errors are unavailable. These are conditional summaries, not multiple-imputation draws. Ordered-predictor cutpoints are retained in <code>fit$missing_data$predictors$x$cutpoints</code>; ordinary R <code>coef()</code> and <code>vcov()</code> exclude them. The bridge retains all raw covariance coordinates internally.</p><p>The two retained finite-state bridge cases pass transport and public-operation checks, including new-data predictions. Native numerical parity remains open at the unchanged <code>4e-6</code> tolerance; these checks establish neither faster warm workflows nor the full native missing-data interface.</p><h2 id="The-DRModels.jl-side-contract" tabindex="-1">The DRModels.jl-side contract <a class="header-anchor" href="#The-DRModels.jl-side-contract" aria-label="Permalink to &quot;The DRModels.jl-side contract {#The-DRModels.jl-side-contract}&quot;">​</a></h2><p>For the bridge to work, DRModels.jl exposes a stable, marshalling-friendly surface:</p><ul><li><p><strong>Formula</strong> — the R <code>bf(mu = y ~ x, sigma = ~ x, ...)</code> is mapped to DRModels.jl&#39;s <code>bf(...)</code> (see the <a href="./developer-notes/formula-grammar">Formula grammar</a> and Rosetta pages for the exact spelling map);</p></li><li><p><strong>Data</strong> — an R <code>data.frame</code> crosses as a column table (<code>NamedTuple</code> / <code>DataFrame</code>) keyed by the same column names;</p></li><li><p><strong>Result</strong> — <code>drm_bridge()</code> returns a flat dictionary with coefficient names and values, covariance matrix, likelihood summaries, convergence state, fitted values, residuals, fitted scale, residual-correlation payloads when present, and direct q=2/q=4 point-export payloads when the exact fitted cell supplies them. The direct exports carry their own claim-boundary strings and remain point/export evidence, not interval or coverage evidence.</p></li></ul><p>For the Gaussian phylogenetic mean cell, the current <code>algorithm = :auto</code> route uses an all-node sparse L-BFGS fitter. That route profiles the mean coefficients by sparse GLS, uses exact Takahashi trace gradients for the residual and phylogenetic standard deviations, and returns a finite mean-coefficient covariance block. Scale and variance-component covariance are still left unset for the R bridge, so profile/bootstrap work remains the next inference slice.</p><h2 id="R-formula-constructs-through-engine-&quot;julia&quot;" tabindex="-1">R formula constructs through <code>engine = &quot;julia&quot;</code> <a class="header-anchor" href="#R-formula-constructs-through-engine-&quot;julia&quot;" aria-label="Permalink to &quot;R formula constructs through \`engine = &quot;julia&quot;\` {#R-formula-constructs-through-engine-&quot;julia&quot;}&quot;">​</a></h2><p>R&#39;s formula mini-language is not Julia&#39;s. <code>@formula</code> cannot evaluate <code>poly(x, 3)</code> or <code>factor(g)</code> the way an R user means them, so the bridge <strong>rewrites</strong> each construct into materialised columns or an expanded term list <em>before</em> handing the formula to <code>@formula</code>. Every construct below is either implemented with a matched R-parity reference on byte-identical data, or rejected for a measured reason.</p><table tabindex="0"><thead><tr><th style="text-align:right;">construct</th><th style="text-align:right;">status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>I(expr)</code></td><td style="text-align:right;">supported, over a safe <code>+ - * / ^</code> grammar only — never arbitrary code</td></tr><tr><td style="text-align:right;"><code>scale(x)</code></td><td style="text-align:right;">supported; centres and scales by the sample mean/SD (R&#39;s default, <code>n-1</code> denominator)</td></tr><tr><td style="text-align:right;"><code>factor(g)</code></td><td style="text-align:right;">supported; levels ordered by <code>sort(unique(...))</code> on the original values, matching R&#39;s <code>contr.treatment</code> baseline</td></tr><tr><td style="text-align:right;"><code>(...)^k</code></td><td style="text-align:right;">supported for a literal positive integer <code>k</code> over a <code>+</code>-only expression</td></tr><tr><td style="text-align:right;"><code>- term</code></td><td style="text-align:right;">supported, including general term removal</td></tr><tr><td style="text-align:right;"><code>poly(x, k)</code></td><td style="text-align:right;">supported — R&#39;s <strong>orthogonal</strong> basis (<code>raw = FALSE</code>, the default), expanding to <code>k</code> columns</td></tr><tr><td style="text-align:right;"><code>poly(x, k, raw = TRUE)</code></td><td style="text-align:right;">rejected — write the powers explicitly with <code>I(x^k)</code></td></tr><tr><td style="text-align:right;"><code>poly()</code> inside <code>(...)^k</code></td><td style="text-align:right;"><strong>rejected on measured evidence</strong>, see below</td></tr><tr><td style="text-align:right;"><code>poly()</code> inside a scalar function</td><td style="text-align:right;">rejected, e.g. <code>log1p(poly(x, 2))</code></td></tr><tr><td style="text-align:right;"><code>poly(x, y, degree)</code>, explicit <code>coefs =</code></td><td style="text-align:right;">rejected — precompute in R and pass the columns</td></tr></tbody></table><h3 id="Two-rejections-worth-explaining" tabindex="-1">Two rejections worth explaining <a class="header-anchor" href="#Two-rejections-worth-explaining" aria-label="Permalink to &quot;Two rejections worth explaining {#Two-rejections-worth-explaining}&quot;">​</a></h3><p><code>poly()</code> is the only construct that rewrites to a <strong>group</strong> of terms, and a group does not compose everywhere a single column does.</p><p><strong>Inside <code>(...)^k</code>.</strong> R treats <code>poly(x, 2)</code> as <strong>one term</strong>, so <code>(x + poly(x, 2))^2</code> crosses two terms and never forms <code>poly1:poly2</code>. Measured against <code>model.matrix()</code> on both sides: R produces <strong>6</strong> model-matrix columns, the flattened rewrite produces <strong>7</strong> — the extra one being exactly that interaction. Rejected rather than special-cased, because keeping the group intact through the power algebra needs a term-grouping concept this rewrite does not have.</p><p><strong>Inside a scalar function.</strong> R maps the function elementwise over poly&#39;s <code>k</code>-column matrix, giving <code>k</code> columns; the rewrite would map it over their <strong>sum</strong>, giving one. Silent, and of exactly the shape a bridge exists to prevent.</p><p>Where poly <em>does</em> compose, it was checked rather than assumed — <code>x * poly(x, 2)</code>, <code>z : poly(x, 2)</code>, <code>poly(x, 2) + z</code> and <code>x + z - poly(x, 2)</code> all match R&#39;s model-matrix width exactly.</p><h3 id="Materialised-columns-and-newdata" tabindex="-1">Materialised columns and <code>newdata</code> <a class="header-anchor" href="#Materialised-columns-and-newdata" aria-label="Permalink to &quot;Materialised columns and \`newdata\` {#Materialised-columns-and-newdata}&quot;">​</a></h3><p><code>I()</code>, <code>scale()</code>, <code>factor()</code> and <code>poly()</code> use temporary columns internally. The development bridge retains the corresponding formula labels and an explicit mapping to the fitted coordinates. Use the returned public coefficient name, such as <code>I(x^2)</code>, in <code>parm = &quot;fixef:mu:I(x^2)&quot;</code>; do not guess a temporary column number. Existing data columns are never replaced by a generated column.</p><p>The companion R adapter uses the original training terms for supported fixed-effect <code>predict(..., newdata = ...)</code> calls. In particular, <code>scale()</code> keeps the training mean and standard deviation, and <code>poly()</code> keeps its training orthogonal basis. Rebuilding either basis on the new rows would change the prediction. This R adapter behavior does not imply that a direct Julia <code>DrmFit</code> can reconstruct arbitrary materialised columns for new data.</p><p>These changes require the matching development versions of both packages. Legacy bridge objects without label metadata keep their existing names. Ambiguous or incomplete label metadata is rejected. This is a coefficient identity contract; it does not establish interval coverage or large-tree profile performance.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DRModels</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x_labels </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> collect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">range</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; length </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 48</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y_labels </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.4</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x_labels </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x_labels</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .+</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">           0.15</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> .*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">collect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">48</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">label_fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> drm_bridge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(formula </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;y ~ x + I(x^2); sigma ~ 1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    family </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;gaussian&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (; y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y_labels, x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x_labels))</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;mu_I(x^2)&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> label_fit[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coef_names&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> label_fit[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coef_names&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> label_fit[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;vcov_names&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">label_fit[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;coef_names&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">4-element Vector{String}:</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;mu_(Intercept)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;mu_x&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;mu_I(x^2)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;"> &quot;sigma_(Intercept)&quot;</span></span></code></pre></div><h2 id="Coefficient-scale-parity-checks" tabindex="-1">Coefficient-scale parity checks <a class="header-anchor" href="#Coefficient-scale-parity-checks" aria-label="Permalink to &quot;Coefficient-scale parity checks {#Coefficient-scale-parity-checks}&quot;">​</a></h2><p>The bridge parity gate fits the admitted cohort (the original six, four fixed-effect families, and <code>nbinom2-dispersion</code>) through <code>drm_bridge</code> and compares coefficients against committed R reference values. Its default tolerances are <code>atol_coef=1e-6</code> and <code>rtol_coef=1e-4</code>, with documented case-specific overrides. Native <code>drm()</code> parity runs alongside it. Cross-package cross-family estimands remain out of scope.</p><p>MIT/GPL: fixtures are <strong>generated numeric outputs only</strong> — never vendored drmTMB source.</p><h2 id="Open-design-questions" tabindex="-1">Open design questions <a class="header-anchor" href="#Open-design-questions" aria-label="Permalink to &quot;Open design questions {#Open-design-questions}&quot;">​</a></h2><p>The following boundaries remain:</p><ul><li><p><strong>Broader phylo / pedigree / relatedness marshalling</strong> — the first Newick tree slice works for one Gaussian <code>phylo(1 | species)</code> mean term, and the q=2 direct-export branch adds fixture-level <code>K</code> / <code>A</code> evidence. Broad pedigree or relatedness marshalling, <code>Ainv</code>, multiple structured terms, slopes, and non-Gaussian phylogenetic models still need separate parity tests.</p></li><li><p><strong>Result-shape parity</strong> — exact field-by-field equivalence between a native drmTMB fit and the Julia-engine fit, guarded by the R-parity suite and the <code>drm_bridge</code> fixture path.</p></li><li><p><strong>Round-trip <code>bf()</code> formulas</strong> — an R formula and its Julia translation must describe the same model; the parity tests enforce this once R is available in CI.</p></li><li><p><strong>Broader measured speed campaigns</strong> — the six-cell fixture timing is retained and scoped; a q=4 scaling comparison measured repeated p&gt;100 fits head-to-head on Totoro. The earlier extrapolated single-number speedup is <strong>retired</strong>. Other large-n campaigns remain separate (Rose: do not invent unmeasured ratios).</p></li></ul><p>Use the bridge for Gaussian one-response / two-response, the admitted Gaussian phylogenetic smoke runs, the narrow q=2 structured exact-Gaussian fixture cells, and the eleven coefficient-scale fixture families above. Use hand-translation via the Rosetta phrasebook or native <code>drmTMB</code> for remaining families and unsupported formula features.</p>`,56)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
