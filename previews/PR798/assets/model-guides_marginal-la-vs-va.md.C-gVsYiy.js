import{_ as a,o as t,c as i,ao as o}from"./chunks/framework.Chkt5rci.js";const m=JSON.parse('{"title":"Laplace vs variational marginals","description":"","frontmatter":{},"headers":[],"relativePath":"model-guides/marginal-la-vs-va.md","filePath":"model-guides/marginal-la-vs-va.md","lastUpdated":null}'),s={name:"model-guides/marginal-la-vs-va.md"};function n(r,e,l,d,h,p){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="Laplace-vs-variational-marginals" tabindex="-1">Laplace vs variational marginals <a class="header-anchor" href="#Laplace-vs-variational-marginals" aria-label="Permalink to &quot;Laplace vs variational marginals {#Laplace-vs-variational-marginals}&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">Status — Experimental</p><p>A variational (VA / ELBO) marginal is an <strong>opt-in Experimental</strong> alternative for random-intercept <code>(1 | g)</code> models. <strong><code>:LA</code> remains the default.</strong> For a scalar Poisson random intercept, <code>:LA</code> uses fixed, non-adaptive 32-node Gauss–Hermite quadrature, not one-point Laplace or adaptive GHQ. Other <code>:LA</code> routes are not identified by this statement.</p><p><strong>Available scope:</strong> Poisson, Binomial, NegBinomial2, Gamma, and Beta <code>(1 | g)</code> via <code>drm(...; marginal = :VA)</code> (scale families need <code>sigma ~ 1</code>). That <code>loglik</code> is an ELBO, not a Laplace log-likelihood. Mixed LA/VA AIC / LRT errors. Phylo / crossed / correlated slopes / ZI / hurdle / <code>sigma ~ x</code> are not supported. In the evaluated Gamma random-intercept model, LA and VA agree on shape while LA is faster.</p></div><h2 id="What-&quot;the-marginal&quot;-is,-and-why-it-matters" tabindex="-1">What &quot;the marginal&quot; is, and why it matters <a class="header-anchor" href="#What-&quot;the-marginal&quot;-is,-and-why-it-matters" aria-label="Permalink to &quot;What &quot;the marginal&quot; is, and why it matters {#What-&quot;the-marginal&quot;-is,-and-why-it-matters}&quot;">​</a></h2><p>When a model has random effects <code>z</code>, <code>drm</code> does not maximise the joint likelihood of data and random effects directly. It integrates the random effects out, leaving a <em>marginal</em> likelihood that depends only on the fixed parameters and the variance components:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>L(θ) = ∫ p(y | z, θ) p(z | θ) dz.</span></span></code></pre></div><p>That integral has no closed form for non-Gaussian families, so it must be approximated. The quality of the approximation is not a side detail: it is what the dispersion, shape, and zero-inflation parameters are estimated <em>against</em>. A biased marginal biases exactly those parameters.</p><h3 id="The-default-:LA-route" tabindex="-1">The default <code>:LA</code> route <a class="header-anchor" href="#The-default-:LA-route" aria-label="Permalink to &quot;The default `:LA` route {#The-default-:LA-route}&quot;">​</a></h3><p>For a scalar Poisson random intercept <code>(1 | g)</code>, the public default <code>:LA</code> route uses fixed, non-adaptive 32-node Gauss–Hermite quadrature. It is neither one-point Laplace nor adaptive Gauss–Hermite quadrature. A Laplace approximation in general replaces the integrand with a Gaussian centred at the posterior mode of <code>z</code>, matched in curvature (the Hessian) at that mode. It is exact only when the responses are Gaussian, a Gaussian random effect enters the mean linearly, and the residual variance is independent of that random effect; then the integrand is Gaussian. A mean random intercept in that Gaussian model needs no approximation.</p><p>The trouble starts when the integrand is <strong>not</strong> close to Gaussian:</p><ul><li><p><strong>Skewed or heavy-tailed posteriors</strong> — a single mode-plus-curvature match understates the mass in the tail, so the integral, and the variance/shape parameters tied to it, are off.</p></li><li><p><strong>Multimodal posteriors</strong> — LA sees one mode and is blind to the others; which mode it lands on can depend on the optimiser, the OS, or the BLAS.</p></li><li><p><strong>Dispersion / shape / zero-inflation parameters</strong> — these read the <em>shape</em> of the integrand, not just its peak, so they absorb the approximation error first. Mean (location) parameters are comparatively robust.</p></li></ul><h2 id="Why-the-approximation-can-matter" tabindex="-1">Why the approximation can matter <a class="header-anchor" href="#Why-the-approximation-can-matter" aria-label="Permalink to &quot;Why the approximation can matter {#Why-the-approximation-can-matter}&quot;">​</a></h2><p>Related latent-variable models in GLLVModels.jl illustrate two geometries in which a one-mode curvature approximation can be unreliable:</p><ul><li><p><strong>Two-part Gamma shape.</strong> In a two-part (hurdle) Gamma model, the Gamma shape parameter <code>α</code> was recovered roughly <strong>7× too low</strong> under Laplace. The mean was fine; the shape — which is read off the curvature of a skewed positive density — was badly biased.</p></li><li><p><strong>ZINB multimodality.</strong> In a zero-inflated negative binomial model the zero-inflation probability <code>π</code> and the low-count-mean intercept <code>βc</code> trade off: a zero can be &quot;structural&quot; (<code>π</code>) or &quot;a Poisson/NB zero from a small mean&quot; (<code>βc</code>). That gives the marginal <strong>two modes</strong>, and the sign of the count intercept was observed to <strong>flip across OS / BLAS</strong> — a hallmark of LA picking different modes on different platforms.</p></li></ul><p>Neither failure is a bug in the optimiser; both are the geometry the Laplace approximation cannot see. They motivate checking the marginal approximation, but they do not show that DRModels.jl&#39;s experimental VA route fixes those specific models.</p><h2 id="The-variational-VA-/-ELBO-alternative" tabindex="-1">The variational (VA / ELBO) alternative <a class="header-anchor" href="#The-variational-VA-/-ELBO-alternative" aria-label="Permalink to &quot;The variational (VA / ELBO) alternative {#The-variational-VA-/-ELBO-alternative}&quot;">​</a></h2><p>The variational path replaces &quot;find one mode and match curvature&quot; with &quot;fit a whole approximating distribution.&quot; We choose a factorised Gaussian</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>q(z) = N(m, diag(v)),</span></span></code></pre></div><p>and pick <code>m</code> and <code>v</code> to maximise the <strong>evidence lower bound</strong> (ELBO):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ELBO(θ, m, v) = E_q[ log p(y, z | θ) ] − E_q[ log q(z) ]  ≤  log L(θ).</span></span></code></pre></div><p>The ELBO is a <em>provable lower bound</em> on the true log marginal for a fixed variational distribution. Its optimisation can still have local optima, and a factorised Gaussian <code>q</code> need not represent a multimodal posterior. The bound is an objective property, not a guarantee of global optimisation or complete posterior geometry.</p><p>The expectations under a Gaussian <code>q</code> are tractable in the two regimes DRModels.jl needs:</p><ul><li><p><strong>Closed form</strong> when the log-density is linear in the linear predictor <code>η</code> and in <code>e^{±η}</code> — this covers <strong>Poisson</strong> and <strong>Gamma</strong>, because the Gaussian expectations of <code>η</code> and of <code>e^{η}</code> (a log-normal moment) are both analytic.</p></li><li><p><strong>One-dimensional Gauss–Hermite quadrature</strong> for everything else — <strong>Binomial</strong>, <strong>negative binomial</strong>, and <strong>Beta</strong> — where the expectation reduces to a single integral over the scalar <code>η</code>, cheaply and accurately evaluated with a handful of GH nodes.</p></li></ul><p>Because <code>q</code> carries a <em>variance</em> <code>v</code>, not just a location, it represents tail and spread directly, so the shape and dispersion parameters are no longer estimated against a curvature match at a single point.</p><h2 id="When-to-use-which" tabindex="-1">When to use which <a class="header-anchor" href="#When-to-use-which" aria-label="Permalink to &quot;When to use which {#When-to-use-which}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Situation</th><th style="text-align:right;">Recommendation</th></tr></thead><tbody><tr><td style="text-align:right;">Fixed-effects-only model</td><td style="text-align:right;">VA adds nothing — there is no latent integral to approximate.</td></tr><tr><td style="text-align:right;">Gaussian response with a Gaussian RE entering the mean linearly and independent residual variance</td><td style="text-align:right;">VA adds nothing — the marginal is already exact here.</td></tr><tr><td style="text-align:right;">Ordinary Gamma <code>(1|g)</code> shape</td><td style="text-align:right;">LA ≈ VA in the scoped Gamma comparison; <strong>prefer LA</strong> (15–20× faster warm).</td></tr><tr><td style="text-align:right;">Two-part / hurdle / ZINB geometry</td><td style="text-align:right;">VA may help in principle, but <strong>DRModels.jl does not currently support these VA models</strong>.</td></tr><tr><td style="text-align:right;">Speed-critical fits</td><td style="text-align:right;">Route-specific: <code>:LA</code> is the default; Poisson scalar random intercepts use fixed GHQ-32.</td></tr></tbody></table><p>In short: <strong><code>:LA</code> is the default and its numerical implementation is route-specific.</strong> On the public Gamma random-intercept cell, Julia matches the R/TMB pattern: the two marginals agree on <code>α</code> and LA wins on time in the evaluated Gamma comparison. The two-part and zero-inflated models where VA could be most useful are not currently supported here.</p><h2 id="The-public-API-Experimental" tabindex="-1">The public API (Experimental) <a class="header-anchor" href="#The-public-API-Experimental" aria-label="Permalink to &quot;The public API (Experimental) {#The-public-API-Experimental}&quot;">​</a></h2><p>The marginal is selected with <code>marginal</code> (not Gaussian <code>method = :ML/:REML</code>). <code>:LA</code> remains the default integration route; its numerical implementation is route-specific:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default `:LA` route; integration is route-specific</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">drm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; marginal </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :LA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># opt-in variational marginal (Experimental: `(1 | g)` on Poisson / Binomial /</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NegBinomial2 / Gamma / Beta; scale families need sigma ~ 1)</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">drm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; marginal </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :VA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Everything else about the call — the <code>bf(...)</code> formulas, the family, the data — stays the same; only how the random effects are integrated out changes. <code>method = :VA</code> on non-Gaussian families is rejected with a pointer to <code>marginal</code>.</p><h2 id="How-to-assess-the-approximation" tabindex="-1">How to assess the approximation <a class="header-anchor" href="#How-to-assess-the-approximation" aria-label="Permalink to &quot;How to assess the approximation {#How-to-assess-the-approximation}&quot;">​</a></h2><p>The Experimental <code>(1 | g)</code> path is checked against three mathematical expectations with known outcomes, rather than judged only by whether the fitted numbers look plausible:</p><ol><li><p><strong>Variance → 0 collapses to independence.</strong> As the random-effect variance is driven to zero there is nothing left to integrate, so the ELBO equals the ordinary independent log-likelihood. This pins the no-RE limit exactly.</p></li><li><p><strong>ELBO ≤ dense quadrature.</strong> At low latent dimension the true marginal is computed by dense <em>adaptive</em> Gauss–Hermite. The ELBO, being a lower bound, must sit at or below it — never above. (Non-adaptive engine GHQ centred at 0 can sit below the ELBO; that is not a counterexample.)</p></li><li><p><strong>Family limits.</strong> The negative binomial becomes Poisson as its size <code>r → ∞</code>, so NB2-VA should approach Poisson-VA when both are applied to the same simulated data.</p></li></ol><p>On Gamma <code>(1 | g)</code>, the evaluated comparison finds LA ≈ VA on shape <code>α</code> and LA is much faster. VA beyond random intercepts (phylo / crossed / ZI / hurdle) and two-part models is not supported here.</p><h2 id="How-this-differs-from-drmTMB" tabindex="-1">How this differs from drmTMB <a class="header-anchor" href="#How-this-differs-from-drmTMB" aria-label="Permalink to &quot;How this differs from drmTMB {#How-this-differs-from-drmTMB}&quot;">​</a></h2><p>drmTMB is built on TMB, which is <strong>Laplace-only</strong>. Offering a variational marginal alongside LA is therefore a Julia-only option; a VA result should not be interpreted as reproducing drmTMB&#39;s marginal likelihood. The option is most relevant where a one-mode approximation is scientifically questionable, such as two-part shape or ZINB multimodality, although those VA models are not currently supported in DRModels.jl. On ordinary Gamma <code>(1 | g)</code>, the evaluated comparison does <strong>not</strong> show a VA accuracy edge; prefer the default Laplace route, as in R.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><a href="./which-scale">Which scale are you modelling?</a> · <a href="./convergence">Improving convergence</a></li></ul>',38)])])}const u=a(s,[["render",n]]);export{m as __pageData,u as default};
