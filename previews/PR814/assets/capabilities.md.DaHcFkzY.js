import{_ as e,o as a,c as o,ao as i}from"./chunks/framework.Dl28dwd-.js";const u=JSON.parse('{"title":"Detailed capabilities and limits","description":"","frontmatter":{},"headers":[],"relativePath":"capabilities.md","filePath":"capabilities.md","lastUpdated":null}'),d={name:"capabilities.md"};function r(s,t,n,l,c,g){return a(),o("div",null,[...t[0]||(t[0]=[i('<h1 id="Detailed-capabilities-and-limits" tabindex="-1">Detailed capabilities and limits <a class="header-anchor" href="#Detailed-capabilities-and-limits" aria-label="Permalink to &quot;Detailed capabilities and limits {#Detailed-capabilities-and-limits}&quot;">​</a></h1><p>Use this page <strong>after</strong> choosing a scientific route, not as a first tutorial. For a first distributional model, start with <a href="./getting-started">Getting started</a>. For a tree, start with <a href="./tutorials/phylogenetic-models">Phylogenetic structured effects</a>. For Gaussian meta-analysis with supplied sampling variances, start with <a href="./tutorials/meta-analysis">Mean effects and residual heterogeneity</a>.</p><p>This is the detailed map of what <code>DRModels.jl</code> can fit today. It is deliberately conservative: “tested” means the route has a worked example and routine checks for the listed use; “implemented, untested” means it is not yet a recommended analysis route; and “not available” means the request cannot currently be fit with DRModels.jl.</p><p>Status legend:</p><ul><li><p><strong>Tested</strong> — suitable for the listed use after your routine model checks.</p></li><li><p><strong>Impl, untested</strong> — not yet a recommended analysis route.</p></li><li><p><strong>Not available</strong> — this request cannot currently be fit with DRModels.jl.</p></li></ul><p>Use <a href="./model-guides/model-map">What can I fit today?</a> for a model-building route, and <a href="./getting-started">Getting started</a> for runnable syntax.</p><h2 id="Response-families" tabindex="-1">Response families <a class="header-anchor" href="#Response-families" aria-label="Permalink to &quot;Response families {#Response-families}&quot;">​</a></h2><p>Each family below can be fitted with fixed effects. The family-level tests use simulated data with known parameters; comparisons with the optional R bridge are run separately.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Family</th><th style="text-align:right;">Mean-axis random effects</th><th style="text-align:right;">Status and boundary</th></tr></thead><tbody><tr><td style="text-align:right;">Gaussian</td><td style="text-align:right;">intercepts, slopes, correlated, crossed, and structured effects</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Student-t</td><td style="text-align:right;">intercepts and slopes</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">SkewNormal</td><td style="text-align:right;">—</td><td style="text-align:right;"><strong>Tested</strong> fixed effects; random effects are refused</td></tr><tr><td style="text-align:right;">Poisson</td><td style="text-align:right;">intercepts, slopes, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">NegBinomial2</td><td style="text-align:right;">intercepts, slopes, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">TruncatedNegBinomial2</td><td style="text-align:right;">—</td><td style="text-align:right;"><strong>Tested</strong> fixed effects</td></tr><tr><td style="text-align:right;">Beta</td><td style="text-align:right;">intercepts, slopes, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong>; crossed random effects have numerical tests but no complete worked analysis</td></tr><tr><td style="text-align:right;">BetaBinomial</td><td style="text-align:right;">intercepts, slopes, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong>; constant <code>sigma</code> only</td></tr><tr><td style="text-align:right;">Binomial</td><td style="text-align:right;">intercepts, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong>; slope random effects are refused</td></tr><tr><td style="text-align:right;">Gamma</td><td style="text-align:right;">intercepts, slopes, crossed, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong>; crossed random effects have numerical tests but no complete worked analysis</td></tr><tr><td style="text-align:right;">LogNormal</td><td style="text-align:right;">intercepts, slopes, phylogenetic, and known-matrix effects</td><td style="text-align:right;"><strong>Tested</strong>; <code>animal</code> and coordinate-spatial effects are refused</td></tr><tr><td style="text-align:right;">ZeroOneBeta</td><td style="text-align:right;">—</td><td style="text-align:right;"><strong>Tested</strong> fixed effects</td></tr><tr><td style="text-align:right;">Tweedie</td><td style="text-align:right;">intercepts and independent slopes</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">CumulativeLogit (ordinal)</td><td style="text-align:right;">intercepts, independent slopes, and phylogenetic effects</td><td style="text-align:right;"><strong>Tested</strong></td></tr></tbody></table><p><strong>Count modifiers</strong> <code>zi</code> (zero-inflation) and <code>hu</code> (hurdle): implemented in the Poisson/NB2 paths; <strong>Tested</strong>.</p><p><strong>Beta boundary modifiers</strong> <code>zoi</code> / <code>coi</code> (zero-/one-inflation): the <code>ZeroOneBeta()</code> family handles the boundary mass; <strong>Tested</strong>.</p><h2 id="Distributional-location–scale-sub-models" tabindex="-1">Distributional (location–scale) sub-models <a class="header-anchor" href="#Distributional-location–scale-sub-models" aria-label="Permalink to &quot;Distributional (location–scale) sub-models {#Distributional-location–scale-sub-models}&quot;">​</a></h2><p>A formula per distributional parameter is the core grammar: <code>bf(...)</code> gives the mean, scale, and any additional parameter supported by the chosen family their own formulas.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status and boundary</th></tr></thead><tbody><tr><td style="text-align:right;">Gaussian mean μ and scale σ formulas</td><td style="text-align:right;"><strong>Tested</strong>; both intercepts and slopes are recovered.</td></tr><tr><td style="text-align:right;">Non-Gaussian <code>sigma</code> or dispersion formula</td><td style="text-align:right;"><strong>Tested</strong> for families with a dispersion model.</td></tr><tr><td style="text-align:right;">Student-t <code>nu</code> (degrees of freedom) formula</td><td style="text-align:right;"><strong>Tested</strong>.</td></tr><tr><td style="text-align:right;">Random effect on the Gaussian scale axis, <code>sigma ~ (1|g)</code></td><td style="text-align:right;"><strong>Tested</strong> with Gauss–Hermite integration.</td></tr><tr><td style="text-align:right;"><code>sigma(fit)</code> and <code>corpairs(fit)</code></td><td style="text-align:right;"><strong>Tested</strong> post-fit accessors.</td></tr></tbody></table><h2 id="Location–scale–scale-models-LSS,-sd" tabindex="-1">Location–scale–scale models (LSS, <code>sd()</code>) <a class="header-anchor" href="#Location–scale–scale-models-LSS,-sd" aria-label="Permalink to &quot;Location–scale–scale models (LSS, `sd()`) {#Location–scale–scale-models-LSS,-sd}&quot;">​</a></h2><p>A third submodel can put a linear predictor on the log standard deviation of a random effect: <code>sd(group) ~ z</code> or <code>sd(species, phylogenetic) ~ z</code>.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status and boundary</th></tr></thead><tbody><tr><td style="text-align:right;">Plain IID LSS <code>sd(group) ~ z</code></td><td style="text-align:right;"><strong>Tested</strong> under ML and opt-in REML.</td></tr><tr><td style="text-align:right;">Phylogenetic LSS <code>sd(species, phylogenetic) ~ z</code></td><td style="text-align:right;"><strong>Tested</strong> under ML and opt-in REML; large phylogenetic fits use the sparse engine.</td></tr><tr><td style="text-align:right;">Sparse phylogenetic LSS</td><td style="text-align:right;"><strong>Tested.</strong> Request <code>sparse = true</code> or <code>algorithm = :sparse_lbfgs</code>.</td></tr><tr><td style="text-align:right;">Multi-component LSS</td><td style="text-align:right;"><strong>Tested</strong> for multi-IID and IID-plus-phylogenetic structures.</td></tr><tr><td style="text-align:right;">Incomplete responses</td><td style="text-align:right;"><strong>Tested</strong> for <code>missing</code> or <code>NaN</code> outcomes on LSS routes.</td></tr></tbody></table><h2 id="Random-effect-structures" tabindex="-1">Random-effect structures <a class="header-anchor" href="#Random-effect-structures" aria-label="Permalink to &quot;Random-effect structures {#Random-effect-structures}&quot;">​</a></h2><h3 id="Plain-unstructured-random-effects-on-the-mean" tabindex="-1">Plain (unstructured) random effects on the mean <a class="header-anchor" href="#Plain-unstructured-random-effects-on-the-mean" aria-label="Permalink to &quot;Plain (unstructured) random effects on the mean {#Plain-unstructured-random-effects-on-the-mean}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">Structure</th><th style="text-align:right;">Status and boundary</th></tr></thead><tbody><tr><td style="text-align:right;">Gaussian random intercept <code>(1|g)</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Gaussian random slope <code>(x|g)</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Correlated intercept and slope</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Multiple, crossed, or nested grouping factors</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Poisson crossed random effects</td><td style="text-align:right;"><strong>Tested</strong> with sparse Laplace integration</td></tr></tbody></table><h3 id="Structured-random-effects-with-a-known-relatedness-matrix-closed-form-Gaussian" tabindex="-1">Structured random effects with a known relatedness matrix (closed-form Gaussian) <a class="header-anchor" href="#Structured-random-effects-with-a-known-relatedness-matrix-closed-form-Gaussian" aria-label="Permalink to &quot;Structured random effects with a known relatedness matrix (closed-form Gaussian) {#Structured-random-effects-with-a-known-relatedness-matrix-closed-form-Gaussian}&quot;">​</a></h3><p>A structured intercept <code>u ~ N(0, σ_s² K)</code> keeps the Gaussian marginal exactly Gaussian and is fit in closed form (PGLS / matrix-determinant lemma).</p><table tabindex="0"><thead><tr><th style="text-align:right;">Marker</th><th style="text-align:right;">Supplies</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>relmat(1|id)</code></td><td style="text-align:right;">user matrix <code>K</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>animal(1|id)</code></td><td style="text-align:right;">additive-relatedness <code>A</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>phylo(1|species)</code> on the <strong>mean</strong></td><td style="text-align:right;">tree (<code>AugmentedPhy</code> or Newick)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>spatial(1|site)</code></td><td style="text-align:right;">coordinates; <code>K(ρ)=exp(-d/ρ)</code>, with ρ estimated</td><td style="text-align:right;"><strong>Tested</strong></td></tr></tbody></table><p>The Gaussian table above describes the simple intercept route. It does not rule out the supported non-Gaussian phylogenetic mean models or the more specific phylogenetic location–scale routes below; those routes have their own family and inference boundaries.</p><h3 id="Non-Gaussian-phylogenetic-random-intercept-on-the-mean-sparse-Laplace" tabindex="-1">Non-Gaussian phylogenetic random intercept on the mean (sparse Laplace) <a class="header-anchor" href="#Non-Gaussian-phylogenetic-random-intercept-on-the-mean-sparse-Laplace" aria-label="Permalink to &quot;Non-Gaussian phylogenetic random intercept on the mean (sparse Laplace) {#Non-Gaussian-phylogenetic-random-intercept-on-the-mean-sparse-Laplace}&quot;">​</a></h3><p>A <code>phylo(1|species)</code> intercept on the mean for non-Gaussian families uses the verified sparse augmented-state Laplace engine.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Family</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;">Poisson</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">NegBinomial2</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Gamma</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Binomial</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Beta</td><td style="text-align:right;"><strong>Tested</strong>; its gradient tolerance is deliberately looser than 1e-6</td></tr><tr><td style="text-align:right;">BetaBinomial</td><td style="text-align:right;"><strong>Tested</strong>; constant <code>sigma</code> only</td></tr><tr><td style="text-align:right;">CumulativeLogit (ordinal)</td><td style="text-align:right;"><strong>Tested</strong>; intercept-only</td></tr></tbody></table><h2 id="Location–scale-with-a-phylogenetic-random-effect-on-the-scale-q2-route" tabindex="-1">Location–scale with a phylogenetic random effect on the scale (q=2 route) <a class="header-anchor" href="#Location–scale-with-a-phylogenetic-random-effect-on-the-scale-q2-route" aria-label="Permalink to &quot;Location–scale with a phylogenetic random effect on the scale (q=2 route) {#Location–scale-with-a-phylogenetic-random-effect-on-the-scale-q2-route}&quot;">​</a></h2><p>A shared random effect on <strong>both</strong> the mean and log-dispersion axes uses a q=2 augmented Laplace calculation and exact O(p) outer gradient. The full fit, gradient, Wald summaries, and profile-likelihood intervals have regular package-test coverage.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;">Coupled mean and log-dispersion axes for NB2 and Gamma</td><td style="text-align:right;"><strong>Tested</strong> through the public <code>drm()</code> interface</td></tr><tr><td style="text-align:right;">Beta and Beta-binomial kernels</td><td style="text-align:right;">Not available through the public coupled location–scale interface</td></tr><tr><td style="text-align:right;">Poisson and lognormal leaves</td><td style="text-align:right;"><strong>Implemented, untested</strong>; do not rely on them for fitted location–scale models</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">Note</p><p>For <strong>non-Gaussian</strong> responses, coupled location–scale random effects are currently available only for NB2 and Gamma. Other non-Gaussian families can place structured or ordinary random effects on the mean axis, not the scale axis.</p><p><strong>Gaussian has a separate, tested scale-phylogeny route.</strong> Write <code>bf(y ~ … + phylo(1|sp), sigma ~ phylo(1|sp))</code> for the default separate mean- and scale-phylogeny effects. A scale-only phylogeny is also supported. <code>phylo_coupled = true</code> opts into a free mean–scale phylogenetic correlation; the plain two-<code>phylo</code> syntax does not do this. Retrieve the two standard deviations with <code>gaussian_locscale_phylo_sds(fit)</code>; a coupled fit records the correlation in <code>fit.scales[:lambda_cor]</code>, and <code>profile_ci = true</code> adds profile intervals for the two standard deviations.</p><p>This Gaussian route requires a common grouping factor and one phylogenetic structured component; it cannot be combined with additional random effects or <code>meta_V</code>. <code>method = :REML</code> is available for the separate and scale-only blocks, but is refused for the coupled block and for the iid <code>sigma ~ (1|g)</code> route. A non-Gaussian scale-axis-only intercept is not part of the public formula grammar.</p></div><h2 id="Coevolution:-q4-phylogenetic-bivariate-location–scale-model-PLSM" tabindex="-1">Coevolution: q=4 phylogenetic bivariate location–scale model (PLSM) <a class="header-anchor" href="#Coevolution:-q4-phylogenetic-bivariate-location–scale-model-PLSM" aria-label="Permalink to &quot;Coevolution: q=4 phylogenetic bivariate location–scale model (PLSM) {#Coevolution:-q4-phylogenetic-bivariate-location–scale-model-PLSM}&quot;">​</a></h2><p>The selling-point model: a shared phylogenetic random effect on all four axes <code>(μ1, μ2, log σ1, log σ2)</code> with a 4×4 between-species covariance <code>Σ_a</code>, plus a residual correlation ρ12. The sparse-Laplace fit, exact O(p) gradient, and public formula route are <strong>tested</strong>.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>bf(mu1=…, mu2=…, sigma1=…, sigma2=…, rho12=…)</code> with phylogeny</td><td style="text-align:right;"><strong>Tested</strong>; recovers fixed effects and <code>Σ_a</code></td></tr><tr><td style="text-align:right;"><code>relmat</code>, <code>animal</code>, and fixed-range <code>spatial</code> structured providers</td><td style="text-align:right;"><strong>Tested</strong>; spatial range is fixed (default: mean pairwise distance)</td></tr><tr><td style="text-align:right;"><code>vc(fit)</code>, <code>ranef(fit)</code>, and <code>coevolution_cor</code> summaries</td><td style="text-align:right;"><strong>Tested</strong>; <code>fit.ranef.Sigma_a</code> is ordered <code>mu1, mu2, sigma1, sigma2</code></td></tr><tr><td style="text-align:right;">Fixed-effect covariance and Wald standard errors (<code>q4_vcov=true</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Bootstrap intervals for coevolution correlations</td><td style="text-align:right;"><strong>Tested for tree-based phylogeny</strong>; non-tree structured providers return point summaries only</td></tr></tbody></table><h2 id="Structured-q2-bivariate-Gaussian-mu1/mu2-only" tabindex="-1">Structured q=2 bivariate Gaussian (mu1/mu2 only) <a class="header-anchor" href="#Structured-q2-bivariate-Gaussian-mu1/mu2-only" aria-label="Permalink to &quot;Structured q=2 bivariate Gaussian (mu1/mu2 only) {#Structured-q2-bivariate-Gaussian-mu1/mu2-only}&quot;">​</a></h2><p>This is a complete-response exact-Gaussian ML model with matching structured random intercepts on <code>mu1</code> and <code>mu2</code>. It requires the same fixed-effect design on both mean formulas and intercept-only <code>sigma1</code>, <code>sigma2</code>, and <code>rho12</code> formulas. Current support covers point estimates and exported summaries only. It does not cover q2 REML, calibrated intervals, non-Gaussian q2 models, or the full R bridge.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>phylo(1|species)</code> on <code>mu1</code> and <code>mu2</code>, with residual <code>rho12</code></td><td style="text-align:right;"><strong>Tested</strong> through <code>drm()</code> and direct export</td></tr><tr><td style="text-align:right;"><code>relmat(1|id)</code> and <code>animal(1|id)</code> on <code>mu1</code> and <code>mu2</code>, using known <code>K</code> / <code>A</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Fixed-covariance spatial q2 fit</td><td style="text-align:right;">Available only in the documented fixed-covariance example; the range-estimating <code>spatial(...)</code> formula route is rejected</td></tr></tbody></table><h2 id="Bivariate-and-paired-responses-residual-correlation" tabindex="-1">Bivariate and paired responses (residual correlation) <a class="header-anchor" href="#Bivariate-and-paired-responses-residual-correlation" aria-label="Permalink to &quot;Bivariate and paired responses (residual correlation) {#Bivariate-and-paired-responses-residual-correlation}&quot;">​</a></h2><p>The Gaussian, lognormal and Student-t routes here fit two responses jointly with a residual/scatter <code>rho12</code>; <code>associate_pairs</code> instead couples two <em>already-fitted</em> univariate models in a second stage. Only the lognormal route reaches the structured (<code>phylo</code>/<code>relmat</code>/<code>animal</code>/<code>spatial</code>) engines of the two sections above, and it does so by delegation rather than by a second engine.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;">Bivariate Gaussian with residual <code>rho12</code> (<code>cbind</code> / <code>mu1</code>,<code>mu2</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>rho12(fit)</code> accessor</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Bivariate <strong>lognormal</strong> (<code>drm(bf(…), LogNormal())</code>, drmTMB&#39;s <code>biv_lognormal()</code>)</td><td style="text-align:right;"><strong>Tested.</strong> Both responses must be strictly positive and are modeled as bivariate normal on <code>log(Y)</code>: <code>mu1</code>/<code>mu2</code> are log-scale means and <code>rho12</code> is a log-residual correlation, not the raw-scale Pearson correlation. Structured <code>phylo</code> and <code>relmat</code> routes are tested; <code>animal</code> and <code>spatial</code> are implemented but untested on this route. <code>method = :REML</code> is refused for all bivariate LogNormal models.</td></tr><tr><td style="text-align:right;">Bivariate <strong>Student-t</strong> (<code>drm(bf(…, nu = …), Student())</code>, drmTMB&#39;s <code>biv_student()</code>)</td><td style="text-align:right;"><strong>Tested.</strong> <code>sigma1</code>/<code>sigma2</code> are scale parameters, not marginal SDs; <code>rho12</code> is a scatter correlation; and <code>nu = 2 + exp(η)</code>, so <code>nu &gt; 2</code>. One <code>nu</code> is shared by the two responses (it may vary by row via <code>nu ~ x</code>); zero <code>rho12</code> does not imply independence at finite <code>nu</code>. This is a residual-only model: <code>phylo</code>, <code>relmat</code>, <code>animal</code>, <code>spatial</code>, and <code>method = :REML</code> are deliberately refused.</td></tr><tr><td style="text-align:right;"><strong>Staged pair association</strong> — <code>associate_pairs</code> / <code>latent_normal</code> / <code>association</code> / <code>PairAssociation</code> / <code>integration_diagnostics</code> (drmTMB&#39;s <code>associate_pairs()</code>)</td><td style="text-align:right;"><strong>Tested.</strong> This is a two-stage, frozen-margin estimator, not a joint model. It supports <code>gaussian_bernoulli</code>, <code>gaussian_nbinom2</code>, <code>bernoulli_bernoulli</code>, <code>bernoulli_nbinom2</code>, and <code>nbinom2_nbinom2</code>; integration diagnostics are available where numerical integration is used. Its uncertainty ignores margin-estimation error, and it offers no simultaneous association bands or profile intervals. The kernel must be explicit; only <code>association ~ 1</code> is supported; <code>marginal = :AGHQ</code>, non-converged margins, non-Bernoulli binomial margins, and other pair classes are refused.</td></tr><tr><td style="text-align:right;">Cross-family bivariate (different families on <code>y1</code> vs <code>y2</code>)</td><td style="text-align:right;"><strong>Experimental.</strong> <code>drm(bf(...), (Gaussian(), Poisson()); data = …)</code> uses a latent-scale scalar correlation in <code>fit.rho_latent</code>. It has narrow documented evidence and no interval-coverage claim. <code>rho12 ~ x</code> is refused: this route fits a scalar latent correlation, not an observation-specific residual correlation. See <a href="./model-guides/cross-family-methods">Cross-family methods</a>.</td></tr></tbody></table><h2 id="Meta-analysis" tabindex="-1">Meta-analysis <a class="header-anchor" href="#Meta-analysis" aria-label="Permalink to &quot;Meta-analysis {#Meta-analysis}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>gaussian()</code> + <code>meta_V(v)</code> with <strong>known diagonal</strong> sampling variances; τ on the σ intercept</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>meta_V(v)</code> plus random intercepts on the mean: <code>(1 | study)</code>, <code>phylo(1 | sp)</code>, <code>relmat(1 | id)</code>, <code>animal(1 | id)</code>, and sums of these with distinct grouping columns; <code>sigma ~ x</code> allowed</td><td style="text-align:right;"><strong>Tested</strong> (ML). Fits the same model as drmTMB <code>engine = &quot;tmb&quot;</code>: on seven comparison fits the log-likelihoods agree within 3e-10 and the estimates within 3e-9 (relative). A phylo SD is on the raw branch-length scale (× √height = drmTMB&#39;s). Not available: REML, random slopes, <code>spatial()</code>, a <code>sigma</code> random effect, <code>sd(g) ~ …</code>, two fields on one grouping column, missing responses; bootstrap with more than one field refuses.</td></tr><tr><td style="text-align:right;">Bivariate known sampling covariance (<code>meta_vcov_bivariate</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Deprecated <code>meta_known_V</code> parity stub</td><td style="text-align:right;">—</td></tr></tbody></table><h2 id="Inference" tabindex="-1">Inference <a class="header-anchor" href="#Inference" aria-label="Permalink to &quot;Inference {#Inference}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Method</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;">Wald SEs + CIs (observed information)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Profile-likelihood CIs (<code>profile_result</code>, <code>confint(:profile)</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Parametric bootstrap (<code>bootstrap_ci</code>/<code>_summary</code>/<code>_result</code>, serial + threaded)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">REML for fixed-effect Gaussian location–scale and Gaussian mean <code>(1 | g)</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>reml_loglik</code> / <code>ml_loglik</code> / <code>estimation_method</code> accessors</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Epsilon-method bias correction (<code>bias_correct</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><strong>χ̄² (chi-bar-square) boundary inference</strong> (Self–Liang / Stram–Lee mixture)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">REML on q=4 Laplace and Location–Scale–Scale models</td><td style="text-align:right;"><strong>Tested</strong></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">REML scope</p><p><code>method=:REML</code> is opt-in. <strong>ML is the default</strong> (REML likelihoods are not comparable across fixed-effect structures). Supported models are the fixed-effect Gaussian location–scale model; a single Gaussian mean intercept <code>(1 | g)</code>; Location–Scale–Scale models (<code>sd(g) ~ z</code>, <code>sd(species, phylogenetic) ~ z</code>, and multi-component LSS); and the bivariate q=4 location–scale engine. σ-RE, random slopes, and non-Gaussian REML stay rejected. This is not AI-REML.</p><p><strong>Normalisation convention:</strong> every REML route in DRModels.jl now reports the <strong>normalised</strong> Patterson–Thompson restricted log-likelihood, so <code>reml_loglik</code> is directly comparable to lme4&#39;s, glmmTMB&#39;s, TMB&#39;s and drmTMB&#39;s <code>logLik()</code>. The bivariate q=2/q=4 Laplace routes previously omitted the <code>(n_β/2)·log(2π)</code> constant while the fixed-effect location–scale and mean <code>(1 | g)</code> routes included it. That inconsistency has been corrected; all supported REML models now report the same normalised quantity.</p></div><h2 id="Model-comparison-and-accessors" tabindex="-1">Model comparison &amp; accessors <a class="header-anchor" href="#Model-comparison-and-accessors" aria-label="Permalink to &quot;Model comparison &amp; accessors {#Model-comparison-and-accessors}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>lrtest</code>, <code>anova</code>, <code>aicc</code>, <code>weights</code>, <code>update</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>aic</code> / <code>bic</code> / <code>dof</code> / <code>nobs</code> / <code>deviance</code> / <code>dof_residual</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>coef</code> / <code>vcov</code> / <code>confint</code> / <code>stderror</code> / <code>coeftable</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>fixef</code> / <code>re_sd</code> / <code>vc</code> / <code>ranef</code> / <code>sigma</code> / <code>corpairs</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>family</code> accessor</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>heritability</code> / <code>repeatability</code> / <code>icc</code> with delta + profile CIs</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Drop-in parity accessors (StatsAPI surface)</td><td style="text-align:right;"><strong>Tested</strong></td></tr></tbody></table><h2 id="Prediction,-post-fit,-residuals,-simulation" tabindex="-1">Prediction, post-fit, residuals, simulation <a class="header-anchor" href="#Prediction,-post-fit,-residuals,-simulation" aria-label="Permalink to &quot;Prediction, post-fit, residuals, simulation {#Prediction,-post-fit,-residuals,-simulation}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>fitted</code> / <code>residuals</code> / response-scale <code>predict</code></td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>predict_parameters</code> / <code>marginal_parameters</code> / <code>prediction_grid</code> / delta-method prediction SEs</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>simulate</code> / <code>check_drm</code> / Dunn–Smyth quantile residuals</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Visualization data (<code>profile_curve</code> / <code>parameter_surface</code> / <code>corpairs_data</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Drawing (<code>drm_figure</code> / thin <code>plot_*</code>; Confidence Eye on <code>:profile</code>)</td><td style="text-align:right;">Optional with Makie and AlgebraOfGraphics; package checks cover the no-Makie fallback, not rendered figures</td></tr></tbody></table><h2 id="R-Julia-bridge-engine-&quot;julia&quot;" tabindex="-1">R → Julia bridge (engine = &quot;julia&quot;) <a class="header-anchor" href="#R-Julia-bridge-engine-&quot;julia&quot;" aria-label="Permalink to &quot;R → Julia bridge (engine = &quot;julia&quot;) {#R-Julia-bridge-engine-&quot;julia&quot;}&quot;">​</a></h2><p>The optional R bridge for <code>drmTMB(..., engine = &quot;julia&quot;)</code> converts formulas and data into a form that Julia can fit, then converts the result back to R.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>drm_bridge</code> (string/dict/named-tuple formula → fit → flattened <code>Dict</code>); univariate, bivariate, phylo-mean, and narrow q2 structured Gaussian examples</td><td style="text-align:right;"><strong>Tested</strong> for the listed model configurations</td></tr><tr><td style="text-align:right;">q2/q4 direct-export helpers (<code>q2_point_export</code>, <code>q4_point_export</code>)</td><td style="text-align:right;"><strong>Tested</strong> for point summaries only; broader bridge behaviour and interval calibration have not been established</td></tr><tr><td style="text-align:right;"><code>drm_bridge_inference</code> (profile + bootstrap), limited to the Gaussian phylo SD block (<code>param=:resd</code>)</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Newick tree string parsing + small LRU cache</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;">Full R-side glue / <code>engine=&quot;julia&quot;</code> round-trip in drmTMB</td><td style="text-align:right;">(R repo)</td></tr></tbody></table><h2 id="Marginal-method-selection-VA/ELBO" tabindex="-1">Marginal method selection (VA/ELBO) <a class="header-anchor" href="#Marginal-method-selection-VA/ELBO" aria-label="Permalink to &quot;Marginal method selection (VA/ELBO) {#Marginal-method-selection-VA/ELBO}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Capability</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><code>marginal=:LA</code> (Laplace) — the default</td><td style="text-align:right;"><strong>Tested</strong></td></tr><tr><td style="text-align:right;"><code>marginal=:VA</code> Poisson <code>(1|g)</code> public path</td><td style="text-align:right;"><strong>Experimental</strong>; it uses an ELBO approximation, labels the fit <code>:VA</code>, and refuses mixed LA/VA AIC or likelihood-ratio comparisons</td></tr><tr><td style="text-align:right;"><code>marginal=:VA</code> Binomial / NB2 / Gamma / Beta <code>(1|g)</code></td><td style="text-align:right;"><strong>Experimental</strong>; scale families require <code>sigma ~ 1</code></td></tr><tr><td style="text-align:right;"><code>method=:VA</code> on non-Gaussian <code>drm()</code></td><td style="text-align:right;"><strong>Rejected</strong> — choose <code>marginal=:VA</code>; <code>method</code> is ML/REML</td></tr></tbody></table><h2 id="Absent-/-out-of-scope-explicit" tabindex="-1">Absent / out-of-scope (explicit) <a class="header-anchor" href="#Absent-/-out-of-scope-explicit" aria-label="Permalink to &quot;Absent / out-of-scope (explicit) {#Absent-/-out-of-scope-explicit}&quot;">​</a></h2><p>To avoid overclaiming, note these boundaries:</p><ul><li><p><strong>Missing data:</strong> listwise predictor preprocessing is not FIML. Experimental joint missing-predictor routes (<code>mi()</code>, <code>JointDrmFit</code>, <code>JointTwoDrmFit</code>, <code>JointFiniteDrmFit</code>, <code>imputed</code>, and <code>miss_control</code>) are exported for evaluation, but their API and numerics may change and they are not covered by R-parity evidence. Gaussian observed-response masking and missing-response handling for Location–Scale–Scale <code>sd()</code> models are available. General multiple imputation outside the joint-model routes and an <code>na.action</code>-style option are absent.</p></li><li><p><strong>VA/ELBO:</strong> experimental random-intercept VA is limited to <code>(1|g)</code> for Poisson, Binomial, NB2, Gamma, and Beta (<code>sigma ~ 1</code> where applicable). Phylogenetic, crossed, correlated-slope, and zero-inflated/hurdle variants are not available.</p></li><li><p><strong>Experimental prototypes:</strong> experimental optimisation and diagnostic prototypes are not public analysis methods. The supported REML and <code>algorithm = :em</code> routes are listed in the Inference table.</p></li></ul><h2 id="Current-evidence-boundaries" tabindex="-1">Current evidence boundaries <a class="header-anchor" href="#Current-evidence-boundaries" aria-label="Permalink to &quot;Current evidence boundaries {#Current-evidence-boundaries}&quot;">​</a></h2><ul><li><p><code>drm_bridge_inference</code> is tested only for the Gaussian phylogenetic standard deviation block (<code>param=:resd</code>); other bridge inference parameters are not yet supported claims.</p></li><li><p>Coupled q=2 location–scale fitting is public and tested for NB2 and Gamma. Beta and beta-binomial cannot currently be requested through <code>drm()</code> and <code>bf()</code> for this model. Poisson and LogNormal versions are implemented but have not yet been tested as complete analyses.</p></li></ul><hr><p><em>“Tested” supports the listed use, not a package-wide performance or interval-coverage claim. Always assess your fitted model and study design.</em></p>',61)])])}const m=e(d,[["render",r]]);export{u as __pageData,m as default};
