| 2026-09-19 | **redundant `BLAS.set_num_threads(1)` calls in the joint_missing family** (speed6 lane, pre-existing item from 4679936dc §3a) | all twelve `test_joint_missing_*` files in one `-t4` session with only the env pin (`OPENBLAS_NUM_THREADS=1`, no `set_num_threads` anywhere) + negative control at `OPENBLAS_NUM_THREADS=2` | ✅ nine non-restoring `BLAS.set_num_threads(1)` calls removed (bridge, two_bridge, two_predictor, uncertainty, frontend, two_frontend, finite_frontend, finite_factor_coding, finite_prediction); the `@test BLAS.get_num_threads() == 1` that followed eight of them is kept, and is now a real precondition on the suite pin instead of a set-then-check tautology. Kept on purpose: `runtests.jl:35` (the suite pin), `test_lss_sparse_multi_public.jl:387` (restores via `blas_before` + `finally`), `test_inference_blas_pinning.jl:59` (the pinning test itself). GREEN: 741/741 across the twelve files, 2m20s, BLAS reported 1 throughout. Negative control: at BLAS=2 exactly the named `thread budget` check fails in bridge (42 pass / 1 fail), the rest of the file runs, nothing aborts. Measured aside: with `OPENBLAS_NUM_THREADS` unset, OpenBLAS starts at 1 on this Mac (Julia 1.10.0, aarch64), so standalone runs here behave as before; on a machine where it starts higher, a standalone run now fails the named check rather than silently running off the pinned environment. | Claude |
