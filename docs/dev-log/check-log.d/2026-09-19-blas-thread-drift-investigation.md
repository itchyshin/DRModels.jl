| 2026-09-19 | **BLAS thread-drift investigation** (speed6 lane, side investigation) | full `Pkg.test()` with a per-file BLAS guard; prefix bisect K ∈ {40,20,5,0}; `julia --check-bounds=yes` isolation | ✅ no BLAS leak in 474 testsets; the q4 vcov gate failures reproduce bit-for-bit under `--check-bounds=yes` alone (the flag `Pkg.test()` always passes), so the cause is codegen, not test order; `runtests.jl` now pins BLAS=1 at suite start and fails loudly naming any file that leaves the count or a pin scope changed | Shannon |
