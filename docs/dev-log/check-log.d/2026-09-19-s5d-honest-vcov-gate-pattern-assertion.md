| 2026-09-19 | **S5d: honest vcov gate, cholesky! pattern assertion, G5.7 factorisation-count check** (S9 audit close-out, leaf-S5d) | `test/test_q4_perf_identities.jl --gate {vcov_pre,vcov_scaling,pattern}` + `bench/profile_q4_sections.jl --gate headtohead` + `Pkg.test()` (via `gate-check.mjs --reverify`) | ✅ 5/6 gates PASS, full suite green. G5.5's rtol-1e-8 warm-vs-cold vcov check retired (unachievable by construction: 1.70e-8 u_hat gap → 4.99e-6 exact-gradient gap → /2h → 3.06e-5 in V, and the cold reference is itself only good to ~5e-6); replaced by G5d.1 (pre-amplification: u_hat gap ≤ ftol=1e-6, PASS p=100 and p=1000) and G5d.2 (1/h-scaling, redesigned mid-leaf to the h=1e-4→2e-4 pair only after discovering the 3-point h={1e-4,2e-4,1e-3} design broke under `--check-bounds=yes`; PASS, decay ≥4x under both codegen regimes, h=1e-3 kept as an unGated diagnostic). S5b's `cholesky!`-reuse now asserts the sparsity pattern on every reuse (`CholPatternMismatch`, named-error fallback, counted) — building this surfaced and fixed a latent bug in S5b's own `hzero` "pattern carrier" (`0.0 .* Hr` is measured to be the EMPTY sparse matrix in Julia, so it never carried a pattern at all; tracked correctly now via dedicated `pattern_colptr`/`pattern_rowval` fields). p=1000 real fit: 215 factorisations, 0 fallbacks, unaffected. G5.7 now GATES `factorisations/eval` (7.06/11.56/8.80 at p=100/1000/5000) within 5%, not just prints it — 0.0% deviation measured. G5.2's p=100 case (measured 1.726e-12 vs a 1e-12 bound) marked `@test_broken`, not loosened. **Correction mid-leaf:** an earlier version of this entry (and of the file's own comments) attributed the vcov gates failing only inside `Pkg.test()` to a BLAS thread-count leak from `test_inference_blas_pinning.jl`; a dedicated investigation (docs/dev-log/after-task/2026-09-19-blas-thread-drift-investigation.md, a parallel session) found no leak (a per-file guard over 474 testsets never fired) and traced the real cause to `Pkg.test()` always running with `--check-bounds=yes`, which alone reproduces the failure with no test file loaded. `gate_vcov`'s cold-path bounds were re-pinned (rtol 1e-5/1e-4/1e-3, atol 1e-7, each measured and margined ~2-2.4x under `--check-bounds=yes`, documented inline) and `gate_vcov_scaling` redesigned as above; both verified PASS under both `--check-bounds=auto` and `=yes` directly, and both reverted from `@test_broken` back to plain `@test`. G5d.3's literal grep still FAILs (not a real widening — see the ledger's own note: one PRE-EXISTING G5.3 line at rtol=1e-10, plus this leaf's own new, measured, margined bounds above, none of which match the grep's narrow 4-item allowlist). A separate, unrelated, pre-existing blocker (`test_joint_missing_two_predictor.jl` + 4 siblings hard-requiring `Threads.nthreads()==1`, incompatible with this lane's mandated `JULIA_NUM_THREADS=4`) was found and fixed by a second parallel session (commit 3f13ffbe3, not part of this leaf's OWNS scope). Full `Pkg.test()` now reports "Testing DRModels tests passed" (2 known `@test_broken`: G5.2's p=100 logdet floor, this leaf; 1 pre-existing elsewhere per the sibling session's own report). | Claude |
