| 2026-09-19 | **joint_missing thread-budget guards vs the mandated JULIA_NUM_THREADS=4** (speed6 lane, follow-up task_61f1b009) | `julia -t4` include of the five files (RED then GREEN) + full `env JULIA_NUM_THREADS=4 OPENBLAS_NUM_THREADS=1 julia --project=. -e 'using Pkg; Pkg.test()'` | ✅ full `Pkg.test()` under `JULIA_NUM_THREADS=4 OPENBLAS_NUM_THREADS=1` reaches `Testing DRModels tests passed` for the first time on this lane: 514 top-level testsets, 0 fail, 0 error, 1 broken (S5d's own `@test_broken` in q4 perf identities), 38 min wall (15:15 to 15:53, Julia 1.10.0, Mac Studio). Root cause: five `test_joint_missing_*` files asserted `Threads.nthreads() == 1` (three as a bare module-level `error("wrong thread budget")` in two_predictor/uncertainty/bridge, which aborted the 14 shard-includes after it; two as `@test` in frontend/two_frontend). The clause is a leftover of the 2026-08-30 parity arc's receipt-provenance rule (receipts produced at one Julia and one BLAS thread; its receipt checker's negative fixture was literally "wrong thread budget"), not an invariant of the code under test: no `src/joint_missing*` code branches on `Threads.nthreads()`, none of the five files call the threaded inference paths, and CI never sets `JULIA_NUM_THREADS` so it always ran at 1 and never saw it. Under the lane's mandated `JULIA_NUM_THREADS=4` (`LOOP/lanes/speed6-20260919/GOAL.md:18`) it could never pass. Fix: Julia-thread clause removed; the BLAS==1 half (the suite's real numerical invariant, pinned suite-wide in runtests.jl by 4679936dc) kept as a reported `@test` in a `thread budget` testset, so a mismatch is named, not fatal. RED: `julia -t4` include of bridge aborted at line 3. GREEN: the five files 264/264 at nthreads=4 (46 s), then the full suite. Sibling task_c0bea3c4's premise (a BLAS leak to 2) was already disproved by 4679936dc's per-file guard; this fix depends on nothing from it. Residual for the S5d leaf: its gate-check recorded G5d.6 as FAIL/SIGTERM because this session's `pkill` caught its outer julia; it must be re-recorded from a fresh run (the orchestrator has it). | Claude |
