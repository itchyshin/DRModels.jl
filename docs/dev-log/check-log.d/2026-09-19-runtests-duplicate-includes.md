| 2026-09-19 | **runtests.jl duplicated includes + harness guard** (speed6 lane, pre-existing item from 4679936dc §7a) | new `test/test_runtests_include_list.jl` (RED on the old file, GREEN after) + `Meta.parseall(runtests.jl)` | ✅ nine test files were included twice (a plain `include` block at lines 327-336 and a `_shard_include` block at 337-346, the both-sides merge the file's own MAINTENANCE NOTE warns about; on origin/main since the 2026-09-03 sharding commit 8bc2ce54e). Cost: nine files ran twice per full suite, and the plain block ran in every CI shard (4x) outside the split and the per-file BLAS guard. Merged into one `_shard_include` block of the 11 unique files in the original order (`test_bridge_formula_constructs.jl` and `test_bridge_zi_marginal_mean.jl` were each only on one side). Class fix: `test_runtests_include_list.jl` parses runtests.jl as text in every shard and fails, naming the paths, on any file included twice, any plain `include` of a test file, or any included file that does not exist. RED: 2 failed (10 plain, 9 duplicated), 281 files exist. GREEN: 276/276; 272 sharded includes. Not re-run as a full `Pkg.test()` here: the S5d builder's suite held the lane's one-Julia-process budget; the change removes repeats and moves one file into the shard split, and every file still included is proven present. The next full run (the orchestrator's) is the runtime confirmation. | Claude |
